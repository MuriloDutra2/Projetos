---
phase: 1
plan: "01-02"
title: "Migração: App.tsx usa services + monta DialogProvider + extrai Excluidos & Configuracoes"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/renderer/src/main.tsx
  - src/renderer/src/App.tsx
  - src/renderer/src/views/Excluidos.tsx
  - src/renderer/src/views/Configuracoes.tsx
autonomous: true
requirements_addressed:
  - REF-01
  - REF-02
  - REF-03
must_haves:
  truths:
    - "App.tsx não chama window.api direto em nenhum lugar — todas as chamadas vão por services/"
    - "App.tsx não chama window.electron.ipcRenderer.invoke em nenhum lugar (anti-pattern eliminado)"
    - "DialogProvider está montado em main.tsx envolvendo <App /> — o operador vê os mesmos modais de alert/confirm"
    - "Existe src/renderer/src/views/Excluidos.tsx com a view extraída de App.tsx:1445-1480"
    - "Existe src/renderer/src/views/Configuracoes.tsx com a view extraída de App.tsx:1482-1514"
    - "App.tsx renderiza <Excluidos /> quando view==='excluidos' e <Configuracoes /> quando view==='configuracoes' (em vez de blocos JSX inline)"
    - "Os tipos inline em App.tsx (Ticket, HistoryEntry, ClientRow, SubscriptionInfo, ClientStatement) foram removidos e substituídos por imports de types/domain.ts"
    - "Os 4 fluxos UAT críticos (entrada → saída → cobrança avulsa, mensalista, garagem, devedor) continuam funcionando — operador não vê regressão"
    - "App.tsx encolhe (estimativa: ~1740 linhas pós-extração das duas menores)"
  artifacts:
    - path: "src/renderer/src/views/Excluidos.tsx"
      provides: "View de tickets excluídos como componente isolado"
      exports:
        - "default"
      max_lines: 80
    - path: "src/renderer/src/views/Configuracoes.tsx"
      provides: "View de configuração de impressora como componente isolado"
      exports:
        - "default"
      max_lines: 80
    - path: "src/renderer/src/main.tsx"
      provides: "Mount do <DialogProvider> envolvendo <App />"
      contains: "<DialogProvider>"
    - path: "src/renderer/src/App.tsx"
      provides: "App com call sites migradas para services + provider montado + 2 views extraídas + 1 AlertModal removido"
      contains: "import { getTickets"
  key_links:
    - from: "src/renderer/src/main.tsx"
      to: "src/renderer/src/providers/DialogProvider"
      via: "mount como wrapper de <App />"
      pattern: "<DialogProvider>"
    - from: "src/renderer/src/App.tsx"
      to: "src/renderer/src/services/*"
      via: "imports nomeados em vez de window.api"
      pattern: "from '\\./services/"
    - from: "src/renderer/src/App.tsx"
      to: "src/renderer/src/views/Excluidos"
      via: "switch case view==='excluidos' renderiza <Excluidos />"
      pattern: "<Excluidos\\s*/>"
    - from: "src/renderer/src/App.tsx"
      to: "src/renderer/src/views/Configuracoes"
      via: "switch case view==='configuracoes' renderiza <Configuracoes />"
      pattern: "<Configuracoes\\s*/>"
---

<objective>
Atomic swap: substituir todas as chamadas `window.api.*` e `window.electron.ipcRenderer.invoke('print-*', ...)` em App.tsx por imports tipados de `services/*`, montar o `<DialogProvider>` em main.tsx (e remover o `<AlertModal>` montado em App.tsx para evitar montagem dupla), e extrair as duas views menores e independentes (Excluidos, Configuracoes) como componentes isolados que consomem os services e o `useDialog()` direto.

Purpose: Esta é a wave que prova que a arquitetura desenhada em 01-01 funciona end-to-end. Se algum service estiver com tipo errado, ou se o provider estiver mal montado, descobrimos AGORA — antes de extrair as 5 views maiores. Excluidos e Configuracoes são as menores (~37 e ~32 linhas de JSX cada) e não dependem de outras views, então o risco de regressão é mínimo. As call sites migradas em App.tsx são os ~30 lugares que hoje fazem `window.api.foo(...)` direto — ao final desta wave, REF-02 já está parcialmente cumprido (zero `window.api` em App.tsx exceto como fallback de migração que esta wave elimina).

Output: main.tsx com provider, App.tsx ~1740 linhas (de 1839, -99 das 2 views + tipos movidos), 2 arquivos de view novos, e zero `window.api` direto / zero `window.electron.ipcRenderer` em App.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-refatora-o-do-renderer/01-CONTEXT.md
@.planning/phases/01-refatora-o-do-renderer/01-RESEARCH.md
@.planning/phases/01-refatora-o-do-renderer/01-PATTERNS.md
@.planning/phases/01-refatora-o-do-renderer/01-01-SUMMARY.md
@CLAUDE.md
@TESTES-ANTES-DO-PENDRIVE.md
@src/renderer/src/main.tsx
@src/renderer/src/App.tsx
@src/renderer/src/components/AlertModal.tsx

<interfaces>
<!-- Contratos disponíveis após Plan 01-01 (já criados): -->

src/renderer/src/types/domain.ts:
- export interface Ticket, HistoryEntry, ClientRow, SubscriptionInfo, ClientStatement
- export type View = 'inicio' | 'historico' | 'relatorio' | 'mensalistas' | 'financeiro' | 'excluidos' | 'configuracoes'

src/renderer/src/services/tickets.ts:
- getTickets, createTicket, checkoutTicket, calculateValue, checkPlateSubscription, checkPlateWasInToday, excludeTicket, excludeAllActiveTickets

src/renderer/src/services/clients.ts:
- getClients, createClient, updateClient, toggleClientStatus, deleteClient, getClientStatement, renewSubscription

src/renderer/src/services/financial.ts:
- getFinancialHistory, getFinancialSummaryByMethod, exportFinancialCsv

src/renderer/src/services/reports.ts:
- getHistory, getHistoryForDay, getHistoryLast24h, getDailyReport, saveDailyReport, exportDailyReportPdf, getExcludedTickets, excludeAllActiveTickets

src/renderer/src/services/printer.ts:
- printEntry, printExit, printSubscription, getPrinters, getPrinterConfig, savePrinterConfig

src/renderer/src/providers/DialogProvider.tsx:
- export default function DialogProvider({ children }): React.JSX.Element
- export function useDialog(): { showAlert, showConfirm }

Mapping completo de window.api calls em App.tsx (de RESEARCH.md + B5 fix):
| App.tsx método      | Service destino                         |
|---------------------|------------------------------------------|
| getTickets          | services/tickets.ts:getTickets           |
| createTicket        | services/tickets.ts:createTicket         |
| checkoutTicket      | services/tickets.ts:checkoutTicket       |
| calculateValue      | services/tickets.ts:calculateValue       |
| checkPlateSubscription | services/tickets.ts:checkPlateSubscription |
| checkPlateWasInToday | services/tickets.ts:checkPlateWasInToday |
| excludeTicket       | services/tickets.ts:excludeTicket  **(B5 fix — call site em handler de exclusão de ticket individual)** |
| excludeAllActiveTickets | services/tickets.ts:excludeAllActiveTickets |
| getClients          | services/clients.ts:getClients           |
| createClient        | services/clients.ts:createClient (chamado em ModalNovoCliente, não App) |
| updateClient        | services/clients.ts:updateClient         |
| toggleClientStatus  | services/clients.ts:toggleClientStatus   |
| deleteClient        | services/clients.ts:deleteClient         |
| getClientStatement  | services/clients.ts:getClientStatement   |
| getFinancialHistory | services/financial.ts:getFinancialHistory |
| getFinancialSummaryByMethod | services/financial.ts:getFinancialSummaryByMethod |
| exportFinancialCsv  | services/financial.ts:exportFinancialCsv |
| getHistory          | services/reports.ts:getHistory           |
| getHistoryForDay    | services/reports.ts:getHistoryForDay     |
| getHistoryLast24h   | services/reports.ts:getHistoryLast24h    |
| getDailyReport      | services/reports.ts:getDailyReport       |
| saveDailyReport     | services/reports.ts:saveDailyReport      |
| exportDailyReportPdf | services/reports.ts:exportDailyReportPdf |
| getExcludedTickets  | services/reports.ts:getExcludedTickets   |
| getPrinters         | services/printer.ts:getPrinters          |
| getPrinterConfig    | services/printer.ts:getPrinterConfig     |
| savePrinterConfig   | services/printer.ts:savePrinterConfig    |
| ipcRenderer.invoke('print-entry') | services/printer.ts:printEntry (via window.api.printEntry após preload fix da wave 1) |
| ipcRenderer.invoke('print-exit')  | services/printer.ts:printExit |

Estado atual de main.tsx (lido):
```typescript
import { createRoot } from 'react-dom/client'
import App from './App'
import './assets/index.css'

createRoot(document.getElementById('root')!).render(<App />)
```
(11 linhas — só precisa envolver `<App />` em `<DialogProvider>`)

Padrão UAT pós-task (REF-03):
- Entrada de placa avulsa → ticket gerado → impressão dispara
- Saída via clique → cálculo + checkout + impressão dispara
- Navegação para 'excluidos' → lista carrega
- Navegação para 'configuracoes' → seletor de impressora carrega
</interfaces>
</context>

<tasks>

<task id="01-02-01" type="execute" tdd="false">
  <name>Task 1: Migrar App.tsx para usar services + montar DialogProvider</name>
  <files>
    - src/renderer/src/main.tsx (modificar)
    - src/renderer/src/App.tsx (modificar)
  </files>
  <read_first>
    - src/renderer/src/App.tsx (arquivo completo — todas as ~30 call sites de window.api e os 2 sites de window.electron.ipcRenderer)
    - src/renderer/src/main.tsx (arquivo completo — para entender a montagem atual)
    - src/renderer/src/providers/DialogProvider.tsx (criado em 01-01 — assinatura do useDialog)
    - src/renderer/src/services/tickets.ts (criado em 01-01)
    - src/renderer/src/services/clients.ts (criado em 01-01)
    - src/renderer/src/services/financial.ts (criado em 01-01)
    - src/renderer/src/services/reports.ts (criado em 01-01)
    - src/renderer/src/services/printer.ts (criado em 01-01)
    - src/renderer/src/types/domain.ts (criado em 01-01)
  </read_first>
  <action>
**Esta task é uma migração de 4 partes em um único commit. Não pular nenhuma — typecheck só fecha quando todas estão consistentes.**

**Parte A — Atualizar main.tsx para envolver `<App />` com `<DialogProvider>`:**

Substituir o conteúdo de `src/renderer/src/main.tsx` por EXATAMENTE:

```typescript
import { createRoot } from 'react-dom/client'
import App from './App'
import DialogProvider from './providers/DialogProvider'
import './assets/index.css'

createRoot(document.getElementById('root')!).render(
  <DialogProvider>
    <App />
  </DialogProvider>
)
```

**Parte B — Em App.tsx, substituir tipos inline por imports de `types/domain.ts`:**

Localizar o bloco de tipos inline em App.tsx (linhas 27-88 conforme PATTERNS.md). REMOVER as 5 declarações inline (`Ticket`, `HistoryEntry`, `ClientRow`, `SubscriptionInfo`, `ClientStatement`). Adicionar UM import no topo do arquivo (após os imports de bibliotecas, antes dos imports de components):

```typescript
import type { Ticket, HistoryEntry, ClientRow, SubscriptionInfo, ClientStatement, View } from './types/domain'
```

Se a union `View` (`type View = 'inicio' | ...`) já for declarada inline em App.tsx, REMOVER essa declaração também — o import acima já a traz.

**Parte C — Em App.tsx, substituir TODAS as ~30 chamadas `window.api.*` por imports de services:**

No topo do arquivo, adicionar imports nomeados (organizar por service):

```typescript
import {
  getTickets,
  createTicket,
  checkoutTicket,
  calculateValue,
  checkPlateSubscription,
  checkPlateWasInToday,
  excludeTicket,
  excludeAllActiveTickets
} from './services/tickets'
import {
  getClients,
  toggleClientStatus,
  deleteClient,
  getClientStatement
} from './services/clients'
import {
  getFinancialHistory,
  getFinancialSummaryByMethod,
  exportFinancialCsv
} from './services/financial'
import {
  getHistory,
  getHistoryForDay,
  getHistoryLast24h,
  getDailyReport,
  saveDailyReport,
  exportDailyReportPdf,
  getExcludedTickets
} from './services/reports'
import { printEntry, printExit, getPrinters, getPrinterConfig, savePrinterConfig } from './services/printer'
```

Em seguida, substituir cada call site (mapping completo):
- `window.api.getTickets()` → `getTickets()`
- `window.api.createTicket(...)` → `createTicket(...)`
- `window.api.checkoutTicket(...)` → `checkoutTicket(...)`
- `window.api.calculateValue(...)` → `calculateValue(...)`
- `window.api.checkPlateSubscription(placa)` → `checkPlateSubscription(placa)`
- `window.api.checkPlateWasInToday(placa)` → `checkPlateWasInToday(placa)`
- `window.api.excludeTicket({ id, password })` → `excludeTicket({ id, password })` **(B5 fix — call site no handler de exclusão de ticket individual; tipicamente em modalExcluir ou contexto similar; não pular este)**
- `window.api.excludeAllActiveTickets({ password })` → `excludeAllActiveTickets({ password })`
- `window.api.getClients()` → `getClients()`
- `window.api.toggleClientStatus(...)` → `toggleClientStatus(...)`
- `window.api.deleteClient(...)` → `deleteClient(...)`
- `window.api.getClientStatement(id)` → `getClientStatement(id)`
- `window.api.getFinancialHistory()` → `getFinancialHistory()`
- `window.api.getFinancialSummaryByMethod(...)` → `getFinancialSummaryByMethod(...)`
- `window.api.exportFinancialCsv()` → `exportFinancialCsv()`
- `window.api.getHistory()` → `getHistory()`
- `window.api.getHistoryForDay(d)` → `getHistoryForDay(d)`
- `window.api.getHistoryLast24h()` → `getHistoryLast24h()`
- `window.api.getDailyReport(d)` → `getDailyReport(d)`
- `window.api.saveDailyReport(d)` → `saveDailyReport(d)`
- `window.api.exportDailyReportPdf(d)` → `exportDailyReportPdf(d)`
- `window.api.getExcludedTickets()` → `getExcludedTickets()`
- `window.api.getPrinters()` → `getPrinters()`
- `window.api.getPrinterConfig()` → `getPrinterConfig()`
- `window.api.savePrinterConfig(name)` → `savePrinterConfig(name)`

**B5 critical:** Antes de finalizar a task, rodar `grep -n "window\.api\.excludeTicket" src/renderer/src/App.tsx` — se retornar QUALQUER linha, o call site não foi migrado e o grep gate de aceitação vai falhar. Buscar o handler que dispara a exclusão de um ticket individual (geralmente parte de um modal de senha em Excluidos ou em Inicio); substituir pela chamada do service.

**Substituir os DOIS sites do anti-pattern `window.electron.ipcRenderer.invoke(...)`:**
- App.tsx ~linha 440 (em `registerEntryWithType` ou equivalente):
  - `window.electron.ipcRenderer.invoke('print-entry', { id: result.id, placa: plate.toUpperCase(), entrada: result.entrada ?? new Date().toISOString() })`
  - → `printEntry({ id: result.id, placa: plate.toUpperCase(), entrada: result.entrada ?? new Date().toISOString() })`
- App.tsx ~linha 346 (em `handleCheckoutConfirm`):
  - `window.electron.ipcRenderer.invoke('print-exit', { placa: ..., entrada: ..., saida: ..., valor: ..., tempoTotal: ... })`
  - → `printExit({ placa: ..., entrada: ..., saida: ..., valor: ..., tempoTotal: ... })`

**Parte D — Em App.tsx, substituir `alertState`/`confirmState` locais por `useDialog()`:**

App.tsx hoje tem (linhas ~153-164 e helpers ~425-431):
```typescript
const [alertState, setAlertState] = useState<{...}>({ open: false, ... })
const [confirmState, setConfirmState] = useState<{...}>({ open: false, ... })
const showAlert = (title, message, type) => setAlertState({ open: true, title, message, type })
const showConfirm = (title, message, onConfirm) => setConfirmState({ open: true, title, message, onConfirm })
```

REMOVER os 2 `useState` (alertState, confirmState) e os 2 helpers (showAlert, showConfirm).

ADICIONAR no topo do componente App, logo após `function App()` e antes do primeiro `useState`:
```typescript
import { useDialog } from './providers/DialogProvider'
// (no topo do arquivo, junto com os outros imports)

// Dentro de function App():
const { showAlert, showConfirm } = useDialog()
```

REMOVER o JSX do `<AlertModal>` no fim do return do App (existe um para alertState e um para confirmState — `confirmState` está montado como AlertModal com `confirmMode`). Esses dois AlertModais agora são montados pelo DialogProvider e não devem ser duplicados em App.tsx (Pitfall: montagem dupla).

Manter intactos:
- Todas as chamadas `showAlert(...)` e `showConfirm(...)` espalhadas no código (continuam funcionando — agora vêm do hook)
- `view`/`setView` (continua local no App)
- Todo o resto do estado local (modalOpen, tickets, history, etc.) — extração das views é em tasks separadas

Regras:
- Sem `;`, single quotes, 100 cols (Prettier).
- NÃO mudar lógica nenhuma — só substituir o canal de chamada.
- Atomic commit: typecheck só fecha quando A+B+C+D estão todos consistentes.
- A union `View` deve continuar a tipar o `useState<View>` em App.tsx — se você removeu a declaração inline, o import de `./types/domain` deve trazê-la.
  </action>
  <acceptance_criteria>
    - `grep -c "window\\.api\\." src/renderer/src/App.tsx` retorna 0
    - `grep -c "window\\.electron\\.ipcRenderer" src/renderer/src/App.tsx` retorna 0
    - **B5 critical:** `grep -c "window\\.api\\.excludeTicket" src/renderer/src/App.tsx` retorna 0 (call site específico migrado para `excludeTicket`)
    - **B5 critical:** `grep -c "excludeTicket(" src/renderer/src/App.tsx` retorna pelo menos 1 (call site presente, agora via service import)
    - `grep -c "from './services/tickets'" src/renderer/src/App.tsx` retorna 1
    - **B5 critical:** o import de `./services/tickets` em App.tsx contém `excludeTicket` (verificável visualmente; o import bloco lista os 8 símbolos)
    - `grep -c "from './services/clients'" src/renderer/src/App.tsx` retorna 1
    - `grep -c "from './services/financial'" src/renderer/src/App.tsx` retorna 1
    - `grep -c "from './services/reports'" src/renderer/src/App.tsx` retorna 1
    - `grep -c "from './services/printer'" src/renderer/src/App.tsx` retorna 1
    - `grep -c "from './providers/DialogProvider'" src/renderer/src/App.tsx` retorna 1
    - `grep -c "from './types/domain'" src/renderer/src/App.tsx` retorna 1
    - `grep -c "useDialog()" src/renderer/src/App.tsx` retorna 1 (a chamada dentro de function App)
    - `grep -c "interface Ticket {" src/renderer/src/App.tsx` retorna 0 (tipos foram movidos)
    - `grep -c "interface HistoryEntry {" src/renderer/src/App.tsx` retorna 0
    - `grep -c "interface ClientRow {" src/renderer/src/App.tsx` retorna 0
    - `grep -c "interface SubscriptionInfo {" src/renderer/src/App.tsx` retorna 0
    - `grep -c "interface ClientStatement {" src/renderer/src/App.tsx` retorna 0
    - `grep -c "type View " src/renderer/src/App.tsx` retorna 0 (importada de domain.ts)
    - `grep -c "<AlertModal" src/renderer/src/App.tsx` retorna 0 (agora montados em DialogProvider)
    - `grep -c "alertState" src/renderer/src/App.tsx` retorna 0 (state local removido)
    - `grep -c "confirmState" src/renderer/src/App.tsx` retorna 0
    - `grep -c "<DialogProvider>" src/renderer/src/main.tsx` retorna 1
    - `grep -c "import DialogProvider" src/renderer/src/main.tsx` retorna 1
    - `wc -l src/renderer/src/App.tsx` retorna número MENOR que o original 1839 (estimativa: 1750-1820 — depende de quantos imports vs. linhas de tipo removidas)
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
    - `npm test` exits 0 (testes existentes não tocam renderer, devem permanecer verdes)
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\Users\muril\OneDrive\Desktop\Estacionamento software\Projetos\meu-estacionamento" && npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <done>App.tsx não tem `window.api` nem `window.electron.ipcRenderer`; usa `useDialog()` em vez de state local; main.tsx envolve <App /> em <DialogProvider>; gates verdes; window.api.excludeTicket migrado para excludeTicket import (B5).</done>
</task>

<task id="01-02-02" type="execute" tdd="false">
  <name>Task 2: Extrair Excluidos.tsx</name>
  <files>
    - src/renderer/src/views/Excluidos.tsx (novo)
    - src/renderer/src/App.tsx (modificar — remover bloco JSX e state)
  </files>
  <read_first>
    - src/renderer/src/App.tsx (linhas 1445-1480 — bloco JSX da view Excluidos; e a linha onde está `useState<...excludedTickets...>`; e a linha do useEffect que chama getExcludedTickets quando view==='excluidos')
    - src/renderer/src/services/reports.ts (criado em 01-01 — confirmar `getExcludedTickets` exportada)
    - .planning/phases/01-refatora-o-do-renderer/01-PATTERNS.md (seção "src/renderer/src/views/Excluidos.tsx")
  </read_first>
  <action>
**Criar `src/renderer/src/views/Excluidos.tsx`** com EXATAMENTE este shape (o JSX interno é cópia verbatim do bloco em App.tsx:1447-1479):

```typescript
import { useState, useEffect } from 'react'
import { format } from 'date-fns'
import { getExcludedTickets } from '../services/reports'

export default function Excluidos(): React.JSX.Element {
  const [excludedTickets, setExcludedTickets] = useState<{
    id: number
    placa: string
    tipo: string
    entrada: string
    saida: string
  }[]>([])

  useEffect(() => {
    getExcludedTickets().then(setExcludedTickets).catch(console.error)
  }, [])

  return (
    <div className="flex-1 p-6 overflow-y-auto">
      {/* JSX completo de App.tsx:1447-1479 — copiar verbatim, mantendo classes Tailwind, headers da tabela, format(new Date(...), 'dd/MM/yyyy HH:mm'), empty state, hover styles */}
    </div>
  )
}
```

A IMPLEMENTAÇÃO interna (entre `<div className="flex-1 p-6 overflow-y-auto">` e o `</div>` de fechamento) deve ser uma cópia VERBATIM do bloco JSX que existe em App.tsx entre as linhas ~1446 e ~1479. Manter:
- Wrapper externo `<div className="flex-1 p-6 overflow-y-auto">`
- Tabela com classes `bg-gray-800 border border-gray-700 rounded-lg overflow-hidden`
- `<thead>` row: `border-b border-gray-700 bg-gray-700/50`, `<th>` com `text-sm font-semibold text-gray-300`
- Empty state: `<td colSpan={4} className="text-center text-gray-500 ...">`
- Row hover: `hover:bg-gray-700/30`
- Format de data: `format(new Date(t.saida), 'dd/MM/yyyy HH:mm')` (manter exatamente como está no original)

**Em `src/renderer/src/App.tsx`:**
1. REMOVER o `useState` de `excludedTickets` (e seu setter).
2. REMOVER o `useEffect` que chama `getExcludedTickets()` quando `view === 'excluidos'`.
3. REMOVER o bloco JSX inline de view===excluidos (linhas ~1445-1480) e SUBSTITUIR pelo render condicional `{view === 'excluidos' && <Excluidos />}` no mesmo lugar onde o switch de views está.
4. ADICIONAR no topo do arquivo:
   ```typescript
   import Excluidos from './views/Excluidos'
   ```
5. Remover o `import { getExcludedTickets } from './services/reports'` ou MANTER se outro lugar do App ainda usa (verificar primeiro com grep — provavelmente só Excluidos usava). Se nenhum outro lugar usa, remover do `import { ... } from './services/reports'`.

Regras:
- Sem `;`, single quotes (Prettier).
- View file usa `export default function Excluidos()` — convenção de componentes.
- Manter `useState`, `useEffect` locais na view — não usar hooks customizados nesta task (hooks são wave 5).
- O `useEffect` na view não tem mais o guard `if (view === 'excluidos')` — a view só renderiza quando ativa, então o effect roda no mount.
  </action>
  <acceptance_criteria>
    - `src/renderer/src/views/Excluidos.tsx` existe
    - `grep -c "export default function Excluidos" src/renderer/src/views/Excluidos.tsx` retorna 1
    - `grep -c "getExcludedTickets" src/renderer/src/views/Excluidos.tsx` retorna 1 (a chamada no useEffect)
    - `grep -c "from '../services/reports'" src/renderer/src/views/Excluidos.tsx` retorna 1
    - `grep -c "window\\." src/renderer/src/views/Excluidos.tsx` retorna 0 (zero acesso a window.* — só vai por service)
    - `wc -l src/renderer/src/views/Excluidos.tsx` retorna número < 80
    - `grep -c "<Excluidos" src/renderer/src/App.tsx` retorna 1 (no switch de views)
    - `grep -c "import Excluidos from" src/renderer/src/App.tsx` retorna 1
    - `grep -c "useState.*excludedTickets" src/renderer/src/App.tsx` retorna 0 (state removido)
    - `wc -l src/renderer/src/App.tsx` retorna número MENOR que o resultado da task 01-02-01 (mais ~30 linhas removidas)
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\Users\muril\OneDrive\Desktop\Estacionamento software\Projetos\meu-estacionamento" && npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <done>Excluidos.tsx existe e renderiza igual ao bloco original; App.tsx instancia <Excluidos /> e perdeu o state e o useEffect dessa view; gates verdes.</done>
</task>

<task id="01-02-03" type="execute" tdd="false">
  <name>Task 3: Extrair Configuracoes.tsx</name>
  <files>
    - src/renderer/src/views/Configuracoes.tsx (novo)
    - src/renderer/src/App.tsx (modificar — remover bloco JSX e state)
  </files>
  <read_first>
    - src/renderer/src/App.tsx (linhas 1482-1514 — bloco JSX da view Configuracoes; e os useState de `printers`/`selectedPrinter`; e o useEffect que chama getPrinters/getPrinterConfig quando view==='configuracoes'; e o handler do botão "Salvar")
    - src/renderer/src/services/printer.ts (criado em 01-01)
    - src/renderer/src/providers/DialogProvider.tsx (criado em 01-01)
    - .planning/phases/01-refatora-o-do-renderer/01-PATTERNS.md (seção "src/renderer/src/views/Configuracoes.tsx")
  </read_first>
  <action>
**Criar `src/renderer/src/views/Configuracoes.tsx`** com este shape:

```typescript
import { useState, useEffect } from 'react'
import { getPrinters, getPrinterConfig, savePrinterConfig } from '../services/printer'
import { useDialog } from '../providers/DialogProvider'

export default function Configuracoes(): React.JSX.Element {
  const { showAlert } = useDialog()
  const [printers, setPrinters] = useState<{ name: string; displayName: string }[]>([])
  const [selectedPrinter, setSelectedPrinter] = useState('')

  useEffect(() => {
    getPrinters().then(setPrinters).catch(console.error)
    getPrinterConfig().then(setSelectedPrinter).catch(console.error)
  }, [])

  const handleSave = async () => {
    const res = await savePrinterConfig(selectedPrinter)
    if (res.success) {
      showAlert('Sucesso', 'Impressora salva com sucesso!', 'success')
    } else {
      showAlert('Erro', 'Erro ao salvar impressora.', 'error')
    }
  }

  return (
    <div className="flex-1 p-6 overflow-y-auto">
      {/* JSX completo de App.tsx:1483-1513 — copiar verbatim:
          - Card: bg-gray-800 border border-gray-700 rounded-lg p-6 max-w-md
          - <h2> com texto "Configurações" (ou string em pt-BR exato do original)
          - <select value={selectedPrinter} onChange={(e) => setSelectedPrinter(e.target.value)}>
          - <option> mapeando sobre printers
          - <button onClick={handleSave} className="bg-red-600 hover:bg-red-700 ...">Salvar</button>
          - Manter strings em pt-BR exatas do original (operadores não vêem mudança)
      */}
    </div>
  )
}
```

O conteúdo do return DEVE replicar exatamente o JSX em App.tsx:1483-1513. Strings em pt-BR (`Configurações`, `Salvar`, etc.) copiadas verbatim. Apenas o `onClick={handleSave}` substitui o handler inline original (que chamava `window.api.savePrinterConfig` direto + `showAlert`); o handler agora usa o service e o `useDialog`.

**Em `src/renderer/src/App.tsx`:**
1. REMOVER os `useState` de `printers` e `selectedPrinter` (e setters).
2. REMOVER o `useEffect` que chama `getPrinters()`/`getPrinterConfig()` quando `view === 'configuracoes'`.
3. REMOVER o bloco JSX inline de view===configuracoes (linhas ~1482-1514) e SUBSTITUIR por `{view === 'configuracoes' && <Configuracoes />}`.
4. ADICIONAR no topo:
   ```typescript
   import Configuracoes from './views/Configuracoes'
   ```
5. Remover do `import { ... } from './services/printer'` os símbolos `getPrinters, getPrinterConfig, savePrinterConfig` SE não forem mais usados em App.tsx (provavelmente não são — Inicio usa `printEntry`/`printExit`, mas getPrinters/getPrinterConfig/savePrinterConfig só apareciam na view de configurações). Manter `printEntry, printExit` no import porque ainda são usados em App.tsx (Inicio ainda não foi extraído).

Regras:
- Sem `;`, single quotes.
- A view usa `useDialog()` direto — sem prop drilling de `showAlert`.
- Strings de UI em pt-BR copiadas verbatim do original.
  </action>
  <acceptance_criteria>
    - `src/renderer/src/views/Configuracoes.tsx` existe
    - `grep -c "export default function Configuracoes" src/renderer/src/views/Configuracoes.tsx` retorna 1
    - `grep -c "getPrinters\\|getPrinterConfig\\|savePrinterConfig" src/renderer/src/views/Configuracoes.tsx` retorna pelo menos 3 (uma chamada de cada)
    - `grep -c "useDialog" src/renderer/src/views/Configuracoes.tsx` retorna 1
    - `grep -c "from '../services/printer'" src/renderer/src/views/Configuracoes.tsx` retorna 1
    - `grep -c "from '../providers/DialogProvider'" src/renderer/src/views/Configuracoes.tsx` retorna 1
    - `grep -c "window\\." src/renderer/src/views/Configuracoes.tsx` retorna 0
    - `wc -l src/renderer/src/views/Configuracoes.tsx` retorna número < 80
    - `grep -c "<Configuracoes" src/renderer/src/App.tsx` retorna 1
    - `grep -c "import Configuracoes from" src/renderer/src/App.tsx` retorna 1
    - `grep -c "useState.*printers" src/renderer/src/App.tsx` retorna 0
    - `grep -c "useState.*selectedPrinter" src/renderer/src/App.tsx` retorna 0
    - `wc -l src/renderer/src/App.tsx` retorna número MENOR que após task 01-02-02 (mais ~35 linhas removidas)
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\Users\muril\OneDrive\Desktop\Estacionamento software\Projetos\meu-estacionamento" && npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <done>Configuracoes.tsx existe, App.tsx instancia <Configuracoes />, state e useEffect movidos; UI em pt-BR preservada; gates verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App.tsx (montagem) ↔ DialogProvider | Atomic swap nesta wave: state local de alertState/confirmState DESCE para o provider e o `<AlertModal>` é montado UMA vez no provider em vez de duas vezes em App. Risco: durante a substituição, ficar com modais montados em ambos os lugares (modal duplicado) ou em nenhum (showAlert silencioso). |
| Renderer ↔ main (IPC, cobertura completa) | Após esta wave, todas as 30 chamadas IPC do renderer passam por services. Se algum service tiver tipagem errada, descobrimos via typecheck ANTES de chegar em produção. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Substituir `window.api.foo(...)` por `foo(...)` errando algum site (ex: passar um argumento a mais ou esquecer um await) | mitigate | `npm run typecheck` no gate de cada task pega assinaturas erradas. Acceptance criterion `grep -c "window\\.api\\." src/renderer/src/App.tsx` retorna 0 garante substituição completa. UAT manual cobre o fluxo principal antes do build final. |
| T-02-02 | Tampering | Trocar `window.electron.ipcRenderer.invoke('print-entry', ...)` por chamada com payload em ordem errada | mitigate | PATTERNS.md mostra os payloads exatos: `{ id, placa, entrada }` para entry e `{ placa, entrada, saida, valor, tempoTotal }` para exit. Tipos no preload (criados em 01-01-02) forçam estrutura correta no typecheck. |
| T-02-03 | Denial of Service | Modal duplicado: AlertModal montado tanto pelo Provider quanto por App.tsx, causando z-index/double-handler | mitigate | Acceptance criterion `grep -c "<AlertModal" src/renderer/src/App.tsx` deve retornar 0 — força remoção do mount original quando o provider passa a ser fonte. |
| T-02-04 | Information Disclosure | Mensagens de erro do operador exibirem detalhes técnicos por causa de showAlert mal substituído | accept | `useDialog().showAlert` tem assinatura idêntica ao helper original `(title, message, type)`. Não há mudança de mensagem; risco zero. |
| T-02-05 | Repudiation | DialogProvider não conseguir mostrar confirm de operação destrutiva (ex: cancelar plano, deletar cliente) | mitigate | Provider monta DOIS AlertModals (alert + confirm com `confirmMode`). Pitfall 6 de RESEARCH.md: `confirmState.onConfirm` continua sendo callback armazenado — funcionamento idêntico ao original. |
| T-02-06 | Elevation of Privilege | Excluidos.tsx ou Configuracoes.tsx introduzir acesso direto a window.api (regressão) | mitigate | Acceptance criterion `grep -c "window\\." src/renderer/src/views/Excluidos.tsx` deve retornar 0 (idem Configuracoes). PATTERNS.md prescreve imports apenas de `services/`. |
| T-02-07 | Tampering | Tipos de domínio movidos para domain.ts mas algum site em App.tsx ainda referencia o tipo inline removido (TS error TS2304) | mitigate | Atomic commit força typecheck a fechar — se faltar a substituição, build trava. Import de domain.ts deve trazer todos os 5 interfaces + View. |
| T-02-08 | Tampering | excludeTicket call site preservar `window.api.excludeTicket` após migração (B5 — call site em handler de exclusão de ticket individual) | mitigate | **B5 fix:** acceptance criteria força `grep -c "window\\.api\\.excludeTicket" App.tsx` retornar 0 e `grep -c "excludeTicket(" App.tsx` retornar pelo menos 1. Mapping table na seção interfaces lista explicitamente o call site. |

## Mitigations

| Threat | Mitigation |
|--------|-----------|
| T-02-01 | Acceptance criteria do task 1: `grep -c "window\\.api" App.tsx == 0`; typecheck obrigatório |
| T-02-02 | Tipos exatos no preload (wave 1); typecheck cobre tsconfig.web.json (renderer) |
| T-02-03 | Acceptance: `grep -c "<AlertModal" App.tsx == 0`; visual UAT da Wave (operador testa um alert e um confirm uma vez) |
| T-02-05 | DialogProvider já monta AlertModal com confirmMode segundo PATTERNS.md; espelho idêntico do código original |
| T-02-06 | grep gates por view file; lint pode adicionar regra futura "no-restricted-globals" para `window.api` em paths fora de services/ — v2 |
| T-02-07 | Atomic commit; `npm run typecheck` cobre tsconfig.web.json |
| T-02-08 | grep gate específico para excludeTicket (B5); mapping table cita explicitamente o call site |
</threat_model>

<verification>
**Gate por task:** `npm run typecheck && npm run lint && npm test`.

**Gate de wave (após as 3 tasks):**
```powershell
Set-Location "C:\Users\muril\OneDrive\Desktop\Estacionamento software\Projetos\meu-estacionamento"
npm run typecheck
npm run lint
npm test
```

```bash
# Greps de sanidade (POSIX shell)
cd "C:/Users/muril/OneDrive/Desktop/Estacionamento software/Projetos/meu-estacionamento"

# REF-02: zero window.api e zero window.electron.ipcRenderer em App.tsx
grep -c "window\.api\." src/renderer/src/App.tsx                  # esperado: 0
grep -c "window\.electron\.ipcRenderer" src/renderer/src/App.tsx  # esperado: 0
grep -c "window\.api\.excludeTicket" src/renderer/src/App.tsx     # esperado: 0 (B5 gate)
grep -c "excludeTicket(" src/renderer/src/App.tsx                 # esperado: >= 1 (B5 gate)

# Provider montado e AlertModal removido do App
grep -c "<DialogProvider>" src/renderer/src/main.tsx              # esperado: 1
grep -c "<AlertModal" src/renderer/src/App.tsx                    # esperado: 0

# Views extraídas
test -f src/renderer/src/views/Excluidos.tsx && echo OK
test -f src/renderer/src/views/Configuracoes.tsx && echo OK
grep -c "<Excluidos" src/renderer/src/App.tsx                     # esperado: 1
grep -c "<Configuracoes" src/renderer/src/App.tsx                 # esperado: 1

# Views novas não tocam window
grep -c "window\." src/renderer/src/views/Excluidos.tsx           # esperado: 0
grep -c "window\." src/renderer/src/views/Configuracoes.tsx       # esperado: 0

# App.tsx encolheu (estimativa)
wc -l src/renderer/src/App.tsx                                    # esperado: < 1820
```

**UAT manual (REF-03) — disparado uma vez ao final da wave. Cross-referência: itens 1-7 e 12-14 do `TESTES-ANTES-DO-PENDRIVE.md` cobrem entrada/saída/cobrança e excluídos/configurações:**
1. `npm run dev`
2. Entrada de placa avulsa → ticket gerado → impressão simulada (verificar console se sem impressora real)
3. Saída via clique → cálculo + checkout + impressão
4. Disparar showAlert via tentativa errada (placa duplicada) → confirma que modal aparece
5. Disparar showConfirm via cancelar plano em Mensalistas → confirma que modal de confirmação aparece
6. Navegar para 'excluidos' → lista carrega
7. Navegar para 'configuracoes' → seletor de impressora carrega + clicar Salvar mostra alert de sucesso
8. **Excluir ticket individual** (B5 regressão check): em Excluidos ou no fluxo do ticket ativo, disparar exclusão com senha → confirmar que o handler funciona idêntico ao pré-refactor
</verification>

<success_criteria>
1. `npm run typecheck`, `npm run lint`, `npm test` ficam verdes ao final.
2. `grep -c "window\\.api" src/renderer/src/App.tsx` retorna 0.
3. `grep -c "window\\.electron\\.ipcRenderer" src/renderer/src/App.tsx` retorna 0.
4. **B5 fix:** `grep -c "window\\.api\\.excludeTicket" src/renderer/src/App.tsx` retorna 0; `excludeTicket` aparece no import bloco e como chamada no handler de exclusão de ticket.
5. `<DialogProvider>` envolve `<App />` em main.tsx e nenhum `<AlertModal>` é montado em App.tsx.
6. Excluidos.tsx e Configuracoes.tsx existem em `src/renderer/src/views/` e são renderizados pelo switch de App.tsx.
7. UAT manual confirma que entrada/saída/cobrança/excluidos/configuracoes/excluir-ticket-individual funcionam idênticos ao pré-refactor (cross-ref TESTES-ANTES-DO-PENDRIVE.md).
8. App.tsx encolheu (mensurável via `wc -l`).
</success_criteria>

<output>
Após completar todas as tasks, criar:

`.planning/phases/01-refatora-o-do-renderer/01-02-SUMMARY.md`

Listar:
- Quantas chamadas window.api foram migradas (esperado: ~30, incluindo excludeTicket)
- Quantos sites de anti-pattern eliminados (esperado: 2)
- Linhas removidas de App.tsx (estimativa: 100-130)
- Status do UAT manual (incluindo item 8 — exclusão de ticket individual, B5)
- Próximo plano: 01-03 — extração das 3 views médias (Historico, Relatorio, Financeiro)
</output>
</content>
</invoke>