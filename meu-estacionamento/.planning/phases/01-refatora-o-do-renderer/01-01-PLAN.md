---
phase: 1
plan: "01-01"
title: "Foundation: tipos, services, preload fix, DialogProvider"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/types/domain.ts
  - src/renderer/src/services/types.ts
  - src/renderer/src/services/tickets.ts
  - src/renderer/src/services/clients.ts
  - src/renderer/src/services/plates.ts
  - src/renderer/src/services/financial.ts
  - src/renderer/src/services/reports.ts
  - src/renderer/src/services/printer.ts
  - src/renderer/src/providers/DialogProvider.tsx
  - src/preload/index.ts
  - src/preload/index.d.ts
autonomous: true
requirements_addressed:
  - REF-02
must_haves:
  truths:
    - "Existem 7 arquivos em src/renderer/src/services/ que envolvem todas as chamadas window.api"
    - "src/renderer/src/types/domain.ts exporta os 5 tipos de domínio compartilhados + a union View"
    - "window.api.printEntry e window.api.printExit estão tipados no preload (fim do anti-pattern)"
    - "DialogProvider existe e expõe useDialog() retornando { showAlert, showConfirm }"
    - "App.tsx ainda funciona sem mudanças nesta wave — wave 2 fará a migração das chamadas"
    - "npm run typecheck e npm run lint ficam verdes"
  artifacts:
    - path: "src/renderer/src/types/domain.ts"
      provides: "Tipos de domínio: Ticket, HistoryEntry, ClientRow, SubscriptionInfo, ClientStatement, View"
      contains: "export interface Ticket"
    - path: "src/renderer/src/services/tickets.ts"
      provides: "Wrappers tipados para 8 chamadas IPC de tickets"
      exports:
        - "getTickets"
        - "createTicket"
        - "checkoutTicket"
        - "calculateValue"
        - "checkPlateSubscription"
        - "checkPlateWasInToday"
        - "excludeTicket"
        - "excludeAllActiveTickets"
    - path: "src/renderer/src/services/clients.ts"
      provides: "Wrappers tipados para 7 chamadas IPC de clientes"
      exports:
        - "getClients"
        - "createClient"
        - "updateClient"
        - "toggleClientStatus"
        - "deleteClient"
        - "getClientStatement"
        - "renewSubscription"
    - path: "src/renderer/src/services/plates.ts"
      provides: "Wrappers tipados para chamadas de placa (separação por entidade conforme D-02)"
      exports:
        - "checkPlateSubscription"
        - "checkPlateWasInToday"
    - path: "src/renderer/src/services/financial.ts"
      provides: "Wrappers tipados financeiros"
      exports:
        - "getFinancialHistory"
        - "getFinancialSummaryByMethod"
        - "exportFinancialCsv"
    - path: "src/renderer/src/services/reports.ts"
      provides: "Wrappers tipados de histórico e relatório diário"
      exports:
        - "getHistory"
        - "getHistoryForDay"
        - "getHistoryLast24h"
        - "getDailyReport"
        - "saveDailyReport"
        - "exportDailyReportPdf"
        - "getExcludedTickets"
        - "excludeAllActiveTickets"
    - path: "src/renderer/src/services/printer.ts"
      provides: "Wrappers tipados de impressora (incluindo printEntry/printExit pós-fix)"
      exports:
        - "printEntry"
        - "printExit"
        - "printSubscription"
        - "getPrinters"
        - "getPrinterConfig"
        - "savePrinterConfig"
    - path: "src/renderer/src/services/types.ts"
      provides: "Re-export dos tipos de domain.ts"
      contains: "export type"
    - path: "src/renderer/src/providers/DialogProvider.tsx"
      provides: "Context provider para alert/confirm globais"
      exports:
        - "default"
        - "useDialog"
    - path: "src/preload/index.ts"
      provides: "api object com printEntry e printExit adicionados"
      contains: "printEntry: (data:"
    - path: "src/preload/index.d.ts"
      provides: "Tipagem Window['api'] com printEntry e printExit declarados"
      contains: "printEntry: (data:"
  key_links:
    - from: "src/renderer/src/services/tickets.ts"
      to: "window.api.getTickets"
      via: "wrapper async retornando Promise<Ticket[]>"
      pattern: "return window\\.api\\."
    - from: "src/renderer/src/services/printer.ts"
      to: "window.api.printEntry / window.api.printExit"
      via: "wrapper que substitui window.electron.ipcRenderer.invoke"
      pattern: "window\\.api\\.print(Entry|Exit)"
    - from: "src/preload/index.ts"
      to: "ipcRenderer.invoke('print-entry'/'print-exit', data)"
      via: "objeto api"
      pattern: "ipcRenderer\\.invoke\\('print-(entry|exit)'"
    - from: "src/renderer/src/providers/DialogProvider.tsx"
      to: "src/renderer/src/components/AlertModal"
      via: "import + render duplo (alert + confirm com confirmMode)"
      pattern: "import AlertModal from"
---

<objective>
Estabelecer a fundação de toda a refatoração: criar a camada de services tipada (única porta para `window.api`), mover os tipos de domínio para um arquivo compartilhado, fechar o anti-pattern de print no preload, e criar o `<DialogProvider>` com `useDialog()`.

Purpose: Sem isso, nenhuma view pode ser extraída de forma limpa — REF-02 ("IPC apenas via hooks/serviços tipados") fica bloqueado e a extração de `Inicio.tsx` continuaria carregando o anti-pattern `window.electron.ipcRenderer.invoke('print-entry'/'print-exit')` para dentro da view nova.

Output: 7 arquivos de service, 1 arquivo de tipos de domínio, 1 re-export, 1 provider, e 2 modificações aditivas no preload (+ tipagem). Nenhum arquivo de view é tocado nesta wave; App.tsx permanece com `window.api.*` direto e o `<AlertModal>` no fim do JSX — a migração é o trabalho da Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-refatora-o-do-renderer/01-CONTEXT.md
@.planning/phases/01-refatora-o-do-renderer/01-RESEARCH.md
@.planning/phases/01-refatora-o-do-renderer/01-PATTERNS.md
@CLAUDE.md
@src/preload/index.ts
@src/preload/index.d.ts

<interfaces>
<!-- Estes são os contratos que as views downstream consumirão. Extraídos do code research. -->

Tipos de domínio (a serem criados em src/renderer/src/types/domain.ts, copiando verbatim de App.tsx:27-88):

```typescript
export interface Ticket {
  id: number
  placa: string
  tipo: string
  entrada: string
  status: string
}

export interface HistoryEntry {
  id: number
  placa: string
  tipo: string
  entrada: string
  saida: string
  valor: number
}

export interface ClientRow {
  id: number
  name: string
  cpf?: string
  phone?: string
  plan_type: string
  expiry_date: string
  status: string
  plates: string[]
  isExpired: boolean
  isDebtor?: boolean
  lastPaymentDate?: string | null
  lastPaymentCompetency?: string | null
  financialStatus?: 'Em dia' | 'Vence hoje' | 'A vencer' | 'Em atraso' | string
  garage_billing_day?: number | null
  garage_billing_month?: number | null
  garageBillingLabel?: string | null
}

export interface SubscriptionInfo {
  isSubscriber: boolean
  clientName: string
  planType: string
  isExpired: boolean
  expiryDate: string
  freeMinutes: number
  isDebtor?: boolean
  clientId?: number
}

export interface ClientStatement {
  client: { id: number; name: string; plan_type: string }
  payments: {
    id: number
    amount: number
    payment_date: string
    payment_method: string
    competency_month?: string | null
    is_advance: number
  }[]
  avulsoWhileDebtor: { id: number; placa: string; saida: string; valor: number }[]
  totals: { payments: number; avulsos: number }
}

export type View = 'inicio' | 'historico' | 'relatorio' | 'mensalistas' | 'financeiro' | 'excluidos' | 'configuracoes'
```

Contrato do AlertModal (já existente, src/renderer/src/components/AlertModal.tsx):
```typescript
// Props que o DialogProvider precisa passar:
// isOpen: boolean
// title: string
// message: string
// type: 'error' | 'success'
// onClose: () => void
// confirmMode?: boolean
// onConfirm?: () => void
// confirmLabel?: string
```

Padrão de wrapper de service (verificado contra src/preload/index.ts):
```typescript
// Cada função em services/ é um async wrapper de uma única chamada window.api.
// Tipo de retorno explícito copiado da declaração em src/preload/index.d.ts.
// Sem try/catch (callers tratam erro).
// Sem React state — funções puras.
```
</interfaces>
</context>

<tasks>

<task id="01-01-01" type="execute" tdd="false">
  <name>Task 1: Criar tipos de domínio + re-export</name>
  <files>
    - src/renderer/src/types/domain.ts (novo)
    - src/renderer/src/services/types.ts (novo)
  </files>
  <read_first>
    - src/renderer/src/App.tsx (linhas 27-88 — tipos inline a serem movidos verbatim)
    - .planning/phases/01-refatora-o-do-renderer/01-PATTERNS.md (seção "src/renderer/src/types/domain.ts")
    - .planning/codebase/CONVENTIONS.md (sem `;`, single quote, named export para tipos)
  </read_first>
  <action>
Criar `src/renderer/src/types/domain.ts` com EXATAMENTE os tipos abaixo (copiados verbatim de App.tsx linhas 27-88, com a adição da union `View`):

```typescript
export interface Ticket {
  id: number
  placa: string
  tipo: string
  entrada: string
  status: string
}

export interface HistoryEntry {
  id: number
  placa: string
  tipo: string
  entrada: string
  saida: string
  valor: number
}

export interface ClientRow {
  id: number
  name: string
  cpf?: string
  phone?: string
  plan_type: string
  expiry_date: string
  status: string
  plates: string[]
  isExpired: boolean
  isDebtor?: boolean
  lastPaymentDate?: string | null
  lastPaymentCompetency?: string | null
  financialStatus?: 'Em dia' | 'Vence hoje' | 'A vencer' | 'Em atraso' | string
  garage_billing_day?: number | null
  garage_billing_month?: number | null
  garageBillingLabel?: string | null
}

export interface SubscriptionInfo {
  isSubscriber: boolean
  clientName: string
  planType: string
  isExpired: boolean
  expiryDate: string
  freeMinutes: number
  isDebtor?: boolean
  clientId?: number
}

export interface ClientStatement {
  client: { id: number; name: string; plan_type: string }
  payments: {
    id: number
    amount: number
    payment_date: string
    payment_method: string
    competency_month?: string | null
    is_advance: number
  }[]
  avulsoWhileDebtor: { id: number; placa: string; saida: string; valor: number }[]
  totals: { payments: number; avulsos: number }
}

export type View = 'inicio' | 'historico' | 'relatorio' | 'mensalistas' | 'financeiro' | 'excluidos' | 'configuracoes'
```

Criar `src/renderer/src/services/types.ts` com EXATAMENTE:

```typescript
export type { Ticket, HistoryEntry, ClientRow, SubscriptionInfo, ClientStatement, View } from '../types/domain'
```

Regras:
- Sem `;` no fim de linhas (Prettier configurado com `semi: false`).
- Single quotes para strings (`'error'`, não `"error"`).
- Named exports apenas (sem `export default` neste arquivo).
- NÃO modificar `App.tsx` ainda — esses tipos continuam declarados inline lá nesta wave; a migração das `import` declarations acontece na wave 2 quando as call sites de window.api forem migradas. Manter os tipos duplicados temporariamente é aceitável para esta wave; typecheck continua verde porque as duas declarações são estruturalmente idênticas.
  </action>
  <acceptance_criteria>
    - `src/renderer/src/types/domain.ts` existe e contém `export interface Ticket`, `export interface HistoryEntry`, `export interface ClientRow`, `export interface SubscriptionInfo`, `export interface ClientStatement`, `export type View`
    - `grep -c "^export" src/renderer/src/types/domain.ts` retorna 6 (5 interfaces + 1 type)
    - `src/renderer/src/services/types.ts` existe e contém `export type { Ticket, HistoryEntry, ClientRow, SubscriptionInfo, ClientStatement, View } from '../types/domain'`
    - `grep -c ";" src/renderer/src/types/domain.ts` retorna 0 fora de bloco-de-tipo (apenas `;` permitidos são separadores intra-objeto como `{ id: number; name: string }` — Prettier mantém)
    - `grep -c '"' src/renderer/src/types/domain.ts` retorna 0 (apenas single quotes)
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\Users\muril\OneDrive\Desktop\Estacionamento software\Projetos\meu-estacionamento" && npm run typecheck && npm run lint</automated>
  </verify>
  <done>Os dois arquivos existem com os exports listados, e typecheck + lint passam.</done>
</task>

<task id="01-01-02" type="execute" tdd="false">
  <name>Task 2: Adicionar printEntry/printExit ao preload (typed)</name>
  <files>
    - src/preload/index.ts (modificar)
    - src/preload/index.d.ts (modificar)
  </files>
  <read_first>
    - src/preload/index.ts (estado atual completo — entender posição do `api` object)
    - src/preload/index.d.ts (estado atual — entender shape do Window['api'])
    - src/renderer/src/App.tsx (linhas 340-360 e 435-455 — sites do anti-pattern `window.electron.ipcRenderer.invoke('print-entry'/'print-exit', ...)` para confirmar payloads)
    - .planning/phases/01-refatora-o-do-renderer/01-PATTERNS.md (seções "src/preload/index.ts" e "src/preload/index.d.ts")
  </read_first>
  <action>
**Adicionar 2 entradas no `api` object** em `src/preload/index.ts`. A inserção é depois da última entrada existente (`printSubscription`, linha 88-92), antes do `}` que fecha o objeto na linha 93. Adicionar EXATAMENTE:

```typescript
  printEntry: (data: { id: number; placa: string; entrada: string }) =>
    ipcRenderer.invoke('print-entry', data),
  printExit: (data: { placa: string; entrada: string; saida: string; valor: number; tempoTotal: string }) =>
    ipcRenderer.invoke('print-exit', data)
```

Atenção: a entrada anterior (`printSubscription`) termina sem vírgula (linha 92, antes do `}`). Adicionar uma vírgula no fim de `printSubscription` para que a próxima entrada não cause erro de parser. Mantém indentação de 2 espaços.

**Adicionar 2 declarações no `Window['api']` interface** em `src/preload/index.d.ts`, depois da declaração de `printSubscription` (linha 98-102) e antes do fechamento `}` na linha 103:

```typescript
      printEntry: (data: { id: number; placa: string; entrada: string }) => Promise<{ success: boolean; error?: string }>
      printExit: (data: { placa: string; entrada: string; saida: string; valor: number; tempoTotal: string }) => Promise<{ success: boolean; error?: string }>
```

Regras:
- Sem `;` no fim de linha (Prettier).
- Indentação de 6 espaços para entradas dentro de `Window['api']` (alinhado com declarações vizinhas).
- NÃO tocar nos handlers do main process — os canais `print-entry` e `print-exit` já existem em `src/main/index.ts` e `src/main/printer.ts`. Esta task apenas expõe os canais já existentes pelo path tipado.
- NÃO remover `window.electron.ipcRenderer.invoke('print-entry'/'print-exit', ...)` em App.tsx ainda — isso é trabalho da wave 2 quando as call sites do Inicio forem migradas. Esta wave só ADICIONA o caminho tipado; o caminho antigo continua funcionando até a wave 2.
  </action>
  <acceptance_criteria>
    - `grep -c "printEntry:" src/preload/index.ts` retorna 1
    - `grep -c "printExit:" src/preload/index.ts` retorna 1
    - `grep -c "ipcRenderer.invoke('print-entry'" src/preload/index.ts` retorna 1
    - `grep -c "ipcRenderer.invoke('print-exit'" src/preload/index.ts` retorna 1
    - `grep -c "printEntry: (data:" src/preload/index.d.ts` retorna 1
    - `grep -c "printExit: (data:" src/preload/index.d.ts` retorna 1
    - `npm run typecheck` exits 0 (cobre tanto tsconfig.node.json — preload — quanto tsconfig.web.json — renderer)
    - `npm run lint` exits 0
    - App.tsx ainda contém `window.electron.ipcRenderer.invoke('print-entry'` (não foi removido nesta task — confirmar)
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\Users\muril\OneDrive\Desktop\Estacionamento software\Projetos\meu-estacionamento" && npm run typecheck && npm run lint</automated>
  </verify>
  <done>Preload e d.ts expõem `printEntry` e `printExit` tipados; typecheck verde nos dois tsconfigs; App.tsx permanece intocado.</done>
</task>

<task id="01-01-03" type="execute" tdd="false">
  <name>Task 3: Criar 7 arquivos de services + DialogProvider</name>
  <files>
    - src/renderer/src/services/tickets.ts (novo)
    - src/renderer/src/services/clients.ts (novo)
    - src/renderer/src/services/plates.ts (novo)
    - src/renderer/src/services/financial.ts (novo)
    - src/renderer/src/services/reports.ts (novo)
    - src/renderer/src/services/printer.ts (novo)
    - src/renderer/src/providers/DialogProvider.tsx (novo)
  </files>
  <read_first>
    - src/preload/index.ts (após task 2 — usar como autoridade dos métodos disponíveis em `api`)
    - src/preload/index.d.ts (após task 2 — usar como autoridade dos tipos de retorno; **NOTA: a tabela em RESEARCH.md ainda lista 6 métodos para clients — usar src/preload/index.d.ts como fonte autoritativa que tem 7 métodos: getClients, createClient, updateClient, toggleClientStatus, deleteClient, getClientStatement, renewSubscription**)
    - src/renderer/src/components/AlertModal.tsx (entender props para DialogProvider montar correto)
    - src/renderer/src/types/domain.ts (após task 1 — importar tipos)
    - .planning/phases/01-refatora-o-do-renderer/01-PATTERNS.md (todas as seções de services e a seção DialogProvider)
    - .planning/codebase/CONVENTIONS.md (export default para componentes; named para helpers; sem barrel)
  </read_first>
  <action>
**Criar `src/renderer/src/services/tickets.ts`** com EXATAMENTE estes 8 named exports (cada um async wrapper sobre window.api, com tipo de retorno copiado de `src/preload/index.d.ts`):

```typescript
import type { Ticket, SubscriptionInfo } from '../types/domain'

export async function getTickets(): Promise<Ticket[]> {
  return window.api.getTickets()
}

export async function createTicket(data: {
  placa: string
  tipo: string
}): Promise<{
  success: boolean
  id?: number
  entrada?: string
  billedAsAvulso?: boolean
  error?: string
  message?: string
}> {
  return window.api.createTicket(data)
}

export async function checkoutTicket(data: {
  id: number
}): Promise<{ success: boolean; valor?: number; error?: string }> {
  return window.api.checkoutTicket(data)
}

export async function calculateValue(data: {
  entrada: string
  placa?: string
  tipo?: string
}): Promise<{ valor: number }> {
  return window.api.calculateValue(data)
}

export async function checkPlateSubscription(
  placa: string
): Promise<SubscriptionInfo & { clientName: string; isSubscriber: boolean; isDebtor: boolean }> {
  return window.api.checkPlateSubscription(placa)
}

export async function checkPlateWasInToday(placa: string): Promise<boolean> {
  return window.api.checkPlateWasInToday(placa)
}

export async function excludeTicket(data: {
  id: number
  password: string
}): Promise<{ success: boolean; error?: string }> {
  return window.api.excludeTicket(data)
}

export async function excludeAllActiveTickets(data: {
  password: string
}): Promise<{ success: boolean; error?: string }> {
  return window.api.excludeAllActiveTickets(data)
}
```

**Criar `src/renderer/src/services/clients.ts`** com EXATAMENTE 7 named exports. **IMPORTANTE — B1 fix:** A tabela em RESEARCH.md lista apenas 6 métodos para clients, mas a fonte autoritativa é `src/preload/index.d.ts` que define 7. O 7º export é `renewSubscription` — NÃO esquecer.

A estrutura completa do arquivo é:

```typescript
import type { ClientRow, ClientStatement } from '../types/domain'

// 1) Listar todos os clientes
export async function getClients(): Promise<ClientRow[]> {
  return window.api.getClients()
}

// 2) Criar cliente
export async function createClient(data: {
  name: string
  cpf: string
  phone: string
  plan_type: string
  expiry_date: string
  plates: string[]
  garage_billing_day?: number | null
  garage_billing_month?: number | null
}): Promise<{ success: boolean; id?: number; error?: string }> {
  return window.api.createClient(data)
}

// 3) Atualizar cliente
export async function updateClient(data: {
  id: number
  name: string
  cpf: string
  phone: string
  plan_type: string
  expiry_date: string
  plates: string[]
  garage_billing_day?: number | null
  garage_billing_month?: number | null
}): Promise<{ success: boolean; error?: string }> {
  return window.api.updateClient(data)
}

// 4) Ativar/desativar cliente
export async function toggleClientStatus(data: {
  clientId: number
  active: number
}): Promise<{ success: boolean; error?: string }> {
  return window.api.toggleClientStatus(data)
}

// 5) Excluir cliente (com senha)
export async function deleteClient(data: {
  clientId: number
  password: string
}): Promise<{ success: boolean; error?: string }> {
  return window.api.deleteClient(data)
}

// 6) Extrato do cliente
export async function getClientStatement(clientId: number): Promise<ClientStatement | null> {
  return window.api.getClientStatement(clientId)
}

// 7) Renovar assinatura — SÉTIMO EXPORT, OBRIGATÓRIO (B1 fix; falta em RESEARCH.md)
// Tipo extraído de src/preload/index.d.ts linha 73
export async function renewSubscription(data: {
  clientId: number
  planType: string
  amount: number
  months?: number
  paymentMethod?: string
  notes?: string
}): Promise<{ success: boolean; newExpiry?: string; error?: string }> {
  return window.api.renewSubscription(data)
}
```

Total: **7 named exports** (`getClients`, `createClient`, `updateClient`, `toggleClientStatus`, `deleteClient`, `getClientStatement`, `renewSubscription`). Se o arquivo final tiver 6 exports, a wave está incorreta — `ModalRenovar` consumirá `renewSubscription` em waves futuras.

**Criar `src/renderer/src/services/plates.ts`** com 2 named exports (separação por entidade conforme D-02):

```typescript
import type { SubscriptionInfo } from '../types/domain'

export async function checkPlateSubscription(
  placa: string
): Promise<SubscriptionInfo & { clientName: string; isSubscriber: boolean; isDebtor: boolean }> {
  return window.api.checkPlateSubscription(placa)
}

export async function checkPlateWasInToday(placa: string): Promise<boolean> {
  return window.api.checkPlateWasInToday(placa)
}
```

Nota: `checkPlateSubscription` e `checkPlateWasInToday` aparecem TANTO em `tickets.ts` QUANTO em `plates.ts` — isso é intencional conforme PATTERNS.md (linha "These two functions are also in services/tickets.ts since the RESEARCH.md assigns them there"). Inicio.tsx importa de `tickets.ts` para usar com createTicket; outras views futuras podem importar de `plates.ts` para domain grouping. Esta duplicação é aceitável (são wrappers triviais, não há lógica duplicada).

**Criar `src/renderer/src/services/financial.ts`** com 3 named exports — `getFinancialHistory`, `getFinancialSummaryByMethod`, `exportFinancialCsv`. Tipos copiados de `src/preload/index.d.ts` linhas 74-75 e 92.

**Criar `src/renderer/src/services/reports.ts`** com 8 named exports — `getHistory`, `getHistoryForDay`, `getHistoryLast24h`, `getDailyReport`, `saveDailyReport`, `exportDailyReportPdf`, `getExcludedTickets`, `excludeAllActiveTickets`. Tipos copiados de `src/preload/index.d.ts` linhas 7-36 e 26.

Nota: `excludeAllActiveTickets` aparece em `tickets.ts` E em `reports.ts` — também intencional conforme RESEARCH.md (Inicio chama via tickets, Excluidos chama via reports). Wrappers idênticos.

**Criar `src/renderer/src/services/printer.ts`** com 6 named exports — `printEntry`, `printExit`, `printSubscription`, `getPrinters`, `getPrinterConfig`, `savePrinterConfig`. Tipos copiados de `src/preload/index.d.ts` (incluindo printEntry/printExit adicionados na task 2).

**Criar `src/renderer/src/providers/DialogProvider.tsx`** com:
- `default export function DialogProvider({ children }: { children: React.ReactNode }): React.JSX.Element` — render do `<DialogContext.Provider>` envolvendo `{children}` e dois `<AlertModal>` (um para alert, outro para confirm com `confirmMode`).
- `named export function useDialog(): DialogContextValue` — `useContext(DialogContext)` com guard "must be used within DialogProvider".
- `DialogContextValue` interface privada (não exportada): `{ showAlert: (title, message, type) => void; showConfirm: (title, message, onConfirm) => void }`.
- State interno: `alertState` shape `{ open: boolean; title: string; message: string; type: 'error' | 'success' }`, `confirmState` shape `{ open: boolean; title: string; message: string; onConfirm: () => void }`.
- Conforme código completo na seção "src/renderer/src/providers/DialogProvider.tsx" do PATTERNS.md (lines 393-460).

Regras gerais para todos os services:
- `window.api` é chamado APENAS dentro de funções de service. Nunca em hooks, providers, views ou components.
- Sem try/catch — callers tratam erro.
- Sem React imports em arquivos de service (tickets.ts, clients.ts, etc.) — apenas `type` imports do domain.ts.
- DialogProvider tem `import { createContext, useContext, useState } from 'react'` e `import AlertModal from '../components/AlertModal'`.
- Sem `;`, single quotes, 100 cols (Prettier).
- Sem barrel `index.ts` em `services/` ou `providers/`.
- Components usam `default export`; services usam `named export`.

NÃO mexer em App.tsx, main.tsx, ou qualquer view/component existente. Esta task apenas CRIA novos arquivos paralelos.
  </action>
  <acceptance_criteria>
    - `src/renderer/src/services/tickets.ts` existe e `grep -c "^export async function" src/renderer/src/services/tickets.ts` retorna 8
    - `src/renderer/src/services/clients.ts` existe e exporta exatamente: `getClients`, `createClient`, `updateClient`, `toggleClientStatus`, `deleteClient`, `getClientStatement`, `renewSubscription` (verificável com `grep -c "^export async function" src/renderer/src/services/clients.ts` retorna **7**)
    - **B1 critical:** `grep -c "^export async function renewSubscription" src/renderer/src/services/clients.ts` retorna 1 (o 7º export está presente)
    - **B1 critical:** `grep -c "renewSubscription" src/renderer/src/services/clients.ts` retorna pelo menos 2 (declaração + chamada window.api.renewSubscription)
    - `src/renderer/src/services/plates.ts` existe e exporta `checkPlateSubscription` e `checkPlateWasInToday`
    - `src/renderer/src/services/financial.ts` existe e exporta `getFinancialHistory`, `getFinancialSummaryByMethod`, `exportFinancialCsv` (3 named exports)
    - `src/renderer/src/services/reports.ts` existe e exporta `getHistory`, `getHistoryForDay`, `getHistoryLast24h`, `getDailyReport`, `saveDailyReport`, `exportDailyReportPdf`, `getExcludedTickets`, `excludeAllActiveTickets` (8 named exports)
    - `src/renderer/src/services/printer.ts` existe e exporta `printEntry`, `printExit`, `printSubscription`, `getPrinters`, `getPrinterConfig`, `savePrinterConfig` (6 named exports)
    - `grep -c "window\\.api\\." src/renderer/src/services/printer.ts` retorna pelo menos 6 (uma chamada por método)
    - `grep -rn "window\\.electron" src/renderer/src/services/` retorna ZERO matches (services nunca tocam ipcRenderer direto)
    - `grep -rn "ipcRenderer" src/renderer/src/services/` retorna ZERO matches
    - `grep -rn "from 'react'" src/renderer/src/services/` retorna ZERO matches (services não importam React)
    - `src/renderer/src/providers/DialogProvider.tsx` existe e contém `export default function DialogProvider` e `export function useDialog`
    - `grep -c "<AlertModal" src/renderer/src/providers/DialogProvider.tsx` retorna 2 (um para alert, um para confirm)
    - Não existe `src/renderer/src/services/index.ts` (sem barrel — D-06)
    - Não existe `src/renderer/src/providers/index.ts` (sem barrel — D-06)
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
    - App.tsx não foi modificado nesta task: `grep -c "import.*from '\\./services/" src/renderer/src/App.tsx` retorna 0
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\Users\muril\OneDrive\Desktop\Estacionamento software\Projetos\meu-estacionamento" && npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <done>Os 7 arquivos de service + DialogProvider existem com os exports corretos (clients.ts com 7 exports incluindo renewSubscription), sem `window.api` fora de services, sem `ipcRenderer` em services, typecheck/lint/tests passam, e App.tsx permanece intocado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC) | Adicionando `printEntry` e `printExit` no allowlist do preload — fluxo: renderer chama `window.api.printEntry(data)` → preload invoke('print-entry') → handler em src/main/printer.ts. O canal `print-entry`/`print-exit` JÁ EXISTE no main; este plano só formaliza o caminho tipado. |
| App.tsx ↔ services (durante esta wave) | App.tsx ainda chama `window.api.*` direto. Os services existem em paralelo. A migração é a wave 2. Não há nova superfície de ataque. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Quebrar contratos IPC ao tipar errado as funções de service | mitigate | Tipos copiados verbatim de `src/preload/index.d.ts` (autoridade) — `npm run typecheck` no gate detecta divergência imediatamente. Audit task 3 acceptance criteria força contagem de exports e correspondência com índice. |
| T-01-02 | Tampering | Adicionar entrada no preload com payload errado (ex: ordem de campos diferente do handler em main/printer.ts) | mitigate | Payloads de `printEntry` e `printExit` copiados verbatim do site existente em App.tsx:340 e 440 (`{ id, placa, entrada }` e `{ placa, entrada, saida, valor, tempoTotal }`). Wave 2 fará migração em commit separado, permitindo rollback se UAT detectar regressão. |
| T-01-03 | Information Disclosure | Service vazar tipo `any` mascarando dados sensíveis | accept | `services/financial.ts` e `services/reports.ts` mantêm `Promise<any[]>` em algumas funções porque o `index.d.ts` atual já usa `any[]`. Tipagem mais forte é trabalho da v2 (TEST-02). Manter paridade com a tipagem existente; não introduzir regressão. |
| T-01-04 | Repudiation | DialogProvider engolir confirmações de operações destrutivas | mitigate | `useDialog().showConfirm` recebe `onConfirm` callback explícito e o AlertModal renderiza no `confirmMode=true` com botão "Confirmar" — comportamento idêntico ao `confirmState` atual em App.tsx:159-164 e 425-427. Operador continua vendo o modal antes da ação. |
| T-01-05 | Denial of Service | Provider montar `<AlertModal>` duplicado (Provider + App.tsx ambos montando) | mitigate | Esta wave NÃO monta o Provider no main.tsx — ela apenas cria o arquivo. A montagem (e remoção do AlertModal de App.tsx) acontece atomicamente na wave 2 (Plan 01-02). Risco zerado nesta wave. |
| T-01-06 | Elevation of Privilege | Anti-pattern `window.electron.ipcRenderer` regredir para dentro de view nova | accept (este plano) / mitigate (próximos planos) | Esta wave não cria nenhuma view nova. Wave 5 (Inicio.tsx) é onde o risco se materializa. Mitigação naquela wave: `grep -rn "window\\.electron" src/renderer/src/views/` deve retornar zero matches. |
| T-01-07 | Tampering | clients.ts publicado com 6 exports (RESEARCH.md desatualizada), quebrando ModalRenovar consumidor de renewSubscription | mitigate | **B1 fix:** Action explicitamente exige 7 exports e mostra renewSubscription completo. Acceptance criterion `grep -c "^export async function" services/clients.ts` retorna **7**. Adicional: `grep -c "^export async function renewSubscription" services/clients.ts` retorna 1. Source-of-truth é `src/preload/index.d.ts`, NÃO RESEARCH.md. |

## Mitigations

| Threat | Mitigation |
|--------|-----------|
| T-01-01 | `npm run typecheck` no gate de cada task; tipos referem-se a `src/renderer/src/types/domain.ts` que é fonte única; lint flag de imports não usados detecta divergências de assinatura |
| T-01-02 | Atomic commit dos 2 arquivos preload juntos (task 2); UAT manual em Wave 2 quando call sites migrarem; `npm run typecheck` cobre tsconfig.node.json (preload) e tsconfig.web.json (renderer) |
| T-01-04 | `<AlertModal confirmMode onConfirm={confirmState.onConfirm} confirmLabel="Confirmar"/>` — código testado contra o uso atual em App.tsx |
| T-01-05 | Não montar provider em main.tsx nesta wave; wave 2 fará atomic swap |
| T-01-07 | grep gate counta 7 exports e nomeia explicitamente renewSubscription |
</threat_model>

<verification>
**Gate por task:** cada task termina com `npm run typecheck && npm run lint`. Task 3 também roda `npm test` (sanity dos 2 testes existentes — devem permanecer verdes; nenhum testa renderer).

**Gate de wave (após todas as 3 tasks):**
```powershell
Set-Location "C:\Users\muril\OneDrive\Desktop\Estacionamento software\Projetos\meu-estacionamento"
npm run typecheck                                            # gate 1
npm run lint                                                 # gate 2
npm test                                                     # gate 3 (calculations + garageDates)
```

```bash
# POSIX equivalentes para os greps de sanidade (rodar em qualquer shell que tenha grep)
cd "C:/Users/muril/OneDrive/Desktop/Estacionamento software/Projetos/meu-estacionamento"
grep -rn "window\.electron" src/renderer/src/services/       # deve ser zero
grep -rn "window\.electron" src/renderer/src/providers/      # deve ser zero
grep -c "printEntry" src/preload/index.ts                    # deve ser >= 1
grep -c "printExit" src/preload/index.d.ts                   # deve ser >= 1
grep -c "^export async function" src/renderer/src/services/clients.ts  # deve ser 7 (B1 gate)
grep -c "renewSubscription" src/renderer/src/services/clients.ts       # deve ser >= 2 (B1 gate)
test -f src/renderer/src/types/domain.ts && echo OK          # arquivo existe
test ! -f src/renderer/src/services/index.ts && echo "no barrel ok"  # sem barrel
```

**Sanidade adicional:** `npm run build` (opcional ao final da wave) — confirma que o bundle sai limpo com os novos arquivos.
</verification>

<success_criteria>
1. `src/renderer/src/types/domain.ts` define os 5 interfaces + `View` union, sendo a única fonte de verdade.
2. Os 7 arquivos de service existem em `src/renderer/src/services/`, cobrem todos os métodos de `window.api`, e nenhum deles usa `window.electron`/`ipcRenderer` direto.
3. `src/renderer/src/services/clients.ts` tem **7 named exports** incluindo `renewSubscription` (B1 fix — RESEARCH.md desatualizado, fonte autoritativa é `src/preload/index.d.ts`).
4. `src/preload/index.ts` e `src/preload/index.d.ts` declaram `printEntry` e `printExit` tipados com os payloads exatos usados hoje em App.tsx:340 e 440.
5. `src/renderer/src/providers/DialogProvider.tsx` exporta `default DialogProvider` + `useDialog`, com dois `<AlertModal>` montados internamente.
6. `App.tsx` permanece sem mudanças — esta wave é puramente aditiva.
7. `npm run typecheck`, `npm run lint`, `npm test` ficam verdes.
</success_criteria>

<output>
Após completar todas as tasks, criar o summary em:

`.planning/phases/01-refatora-o-do-renderer/01-01-SUMMARY.md`

O summary deve listar:
- Arquivos criados (com contagem de exports cada — clients.ts deve listar 7)
- Modificações no preload (linhas exatas adicionadas)
- Confirmação de gates (typecheck/lint/test)
- Próximo plano: 01-02 — migração das call sites de App.tsx + extração das duas views menores (Excluidos, Configuracoes)
</output>
</content>
</invoke>