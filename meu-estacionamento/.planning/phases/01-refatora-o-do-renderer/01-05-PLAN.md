---
phase: 1
plan: "01-05"
title: "Extrair Inicio + criar hooks + cleanup final do App.tsx"
type: execute
wave: 5
depends_on:
  - "01-04"
files_modified:
  - src/renderer/src/views/Inicio.tsx
  - src/renderer/src/hooks/useTickets.ts
  - src/renderer/src/hooks/useGlobalShortcuts.ts
  - src/renderer/src/App.tsx
autonomous: true
requirements_addressed:
  - REF-01
  - REF-02
  - REF-03
must_haves:
  truths:
    - "Existe src/renderer/src/views/Inicio.tsx contendo entrada de placa, lista de tickets ativos, fluxo devedor, garage modal, ModalCheckout, modal excluir todos"
    - "Existe src/renderer/src/hooks/useTickets.ts encapsulando fetch + tick interval (D-09 fix substitui setInterval([...p]) hack)"
    - "Existe src/renderer/src/hooks/useGlobalShortcuts.ts encapsulando o keyboard listener"
    - "App.tsx fica abaixo de 200 linhas — apenas sidebar + router + montagem das views"
    - "App.tsx renderiza <Inicio /> via switch e Inicio recebe setView por prop para o redirect debtor→mensalistas"
    - "Anti-pattern eliminado por completo: grep window.electron.ipcRenderer em todo src/renderer/src retorna 0 matches"
    - "Todos os 7 arquivos de view < 400 linhas (REF-01 fechado)"
    - "Todos os fluxos UAT críticos funcionam (entrada, saída, mensalista, garagem, devedor, barcode scanner, exclusão de todos com senha)"
    - "npm run typecheck, npm run lint, npm test ficam verdes; npm run build sai limpo"
  artifacts:
    - path: "src/renderer/src/views/Inicio.tsx"
      provides: "View principal de entrada/saída de veículos com todos os fluxos"
      exports:
        - "default"
      max_lines: 400
    - path: "src/renderer/src/hooks/useTickets.ts"
      provides: "Hook de tickets com fetch + tick re-render (D-09)"
      exports:
        - "useTickets"
    - path: "src/renderer/src/hooks/useGlobalShortcuts.ts"
      provides: "Hook de keyboard shortcut Ctrl+N"
      exports:
        - "useGlobalShortcuts"
    - path: "src/renderer/src/App.tsx"
      provides: "App enxuto: sidebar + router + view dispatch"
      max_lines: 200
  key_links:
    - from: "src/renderer/src/App.tsx"
      to: "src/renderer/src/views/Inicio"
      via: "switch case render com prop setView"
      pattern: "<Inicio\\s+setView"
    - from: "src/renderer/src/views/Inicio.tsx"
      to: "src/renderer/src/services/printer.ts:printEntry, printExit"
      via: "service calls (sem ipcRenderer direto)"
      pattern: "(printEntry|printExit)\\("
    - from: "src/renderer/src/views/Inicio.tsx"
      to: "src/renderer/src/hooks/useBarcodeScanner.ts (existente)"
      via: "import + chamada com enabled=true"
      pattern: "useBarcodeScanner\\("
    - from: "src/renderer/src/hooks/useTickets.ts"
      to: "src/renderer/src/services/tickets.ts:getTickets"
      via: "fetch + setInterval para re-render"
      pattern: "setInterval"
---

<objective>
Última wave do refactor: extrair `Inicio.tsx` (a view mais complexa, 18+ states e múltiplos fluxos), criar 2 hooks (`useTickets`, `useGlobalShortcuts`) que encapsulam ciclos de vida ainda em App.tsx, e fazer o cleanup final reduzindo App.tsx para um shell de ~150-200 linhas com sidebar e dispatch de view.

Purpose: Inicio é onde mora o coração do produto — entrada de placa, validação de assinatura/devedor, decisão de cobrar avulso ou cobrar mensalidade, modal de garagem, scanner de código de barras, impressão de ticket, modal de checkout, e modal "Excluir todos os ativos" com senha. Esta wave fecha a fase: ao final, REF-01 (≤400 linhas/arquivo), REF-02 (zero IPC direto em components), e REF-03 (gates verdes + UAT) estão todos cumpridos.

Output: Inicio.tsx (~350 linhas estimadas), 2 hooks novos, App.tsx ~150 linhas. Anti-pattern eliminado em todo renderer. Todos os 4 fluxos UAT principais validados manualmente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-refatora-o-do-renderer/01-CONTEXT.md
@.planning/phases/01-refatora-o-do-renderer/01-RESEARCH.md
@.planning/phases/01-refatora-o-do-renderer/01-PATTERNS.md
@.planning/phases/01-refatora-o-do-renderer/01-04-SUMMARY.md
@CLAUDE.md
@TESTES-ANTES-DO-PENDRIVE.md
@src/renderer/src/App.tsx
@src/renderer/src/components/ModalCheckout.tsx
@src/renderer/src/hooks/useBarcodeScanner.ts

<interfaces>
<!-- Disponível após 01-04: -->

src/renderer/src/services/tickets.ts → getTickets, createTicket, checkoutTicket, calculateValue, checkPlateSubscription, checkPlateWasInToday, excludeAllActiveTickets
src/renderer/src/services/printer.ts → printEntry, printExit (substitui ipcRenderer.invoke)
src/renderer/src/providers/DialogProvider.tsx → useDialog()
src/renderer/src/types/domain.ts → Ticket, SubscriptionInfo, View
src/renderer/src/hooks/useBarcodeScanner.ts → useBarcodeScanner(callback, enabled)

State em App.tsx ainda a mover/elevar:
- tickets (compartilhado por Inicio e nada mais — pode descer para Inicio)
- modalOpen, checkoutTicket, checkoutValor, checkoutLoading (estado do ModalCheckout — desce para Inicio)
- modalExcluirTodosOpen, excluirTodosPassword, excluirTodosLoading, excluirTodosError (modal "Excluir todos" — desce para Inicio)
- placa, tipo, loading, subscriptionInfo, plateWasInToday, searchPlacaList (estado de input de placa — desce para Inicio)
- debtorDecisionOpen, pendingEntry (fluxo de devedor — desce para Inicio)
- garageEntryModal (modal de entrada de garagem — desce para Inicio)
- view, setView (continua em App)

Handlers em App.tsx a mover para Inicio:
- handlePlacaBlur (~282)
- handleRegisterEntry, registerEntryWithType (~290-340)
- handleCheckoutClick, handleCheckoutConfirm (~340-380)
- handleBarcodeScanned (~514-531)
- openExcluirTodosConfirm e handler de senha (~410-...)
- handler de fluxo devedor (decidir cobrar avulso ou marcar pago)

useEffect em App.tsx a mover/eliminar:
- setInterval(setTickets((p) => [...p]), 60000) — App.tsx:212 — vira tick em useTickets (D-09)
- useEffect que faz loadTickets — App.tsx:244 — vira useTickets
- useEffect debounce checkPlateWasInToday — App.tsx:~205 — vai para Inicio
- useEffect de keyboard — App.tsx:216-242 — Ctrl+N vai para useGlobalShortcuts; Escape vira tratamento local em cada modal/view conforme RESEARCH.md Open Question 2

ModalCheckout props (já existente):
- isOpen, onClose, ticket, valor, loading, onConfirm

useBarcodeScanner já existe — sem mudanças. Inicio chama `useBarcodeScanner(handleBarcodeScanned, true)` porque a view só renderiza quando ativa.
</interfaces>
</context>

<tasks>

<task id="01-05-01" type="execute" tdd="false">
  <name>Task 1: Criar useTickets e useGlobalShortcuts</name>
  <files>
    - src/renderer/src/hooks/useTickets.ts (novo)
    - src/renderer/src/hooks/useGlobalShortcuts.ts (novo)
  </files>
  <read_first>
    - src/renderer/src/App.tsx (linhas 211-213 setInterval hack; linhas 244-250 loadTickets; linhas 216-242 keyboard effect)
    - src/renderer/src/hooks/useBarcodeScanner.ts (padrão de cleanup com window.addEventListener / removeEventListener)
    - src/renderer/src/services/tickets.ts
    - .planning/phases/01-refatora-o-do-renderer/01-PATTERNS.md (seções "src/renderer/src/hooks/useTickets.ts" e "src/renderer/src/hooks/useGlobalShortcuts.ts")
  </read_first>
  <action>
**Criar `src/renderer/src/hooks/useTickets.ts`** com EXATAMENTE:

```typescript
import { useState, useEffect, useCallback } from 'react'
import { getTickets } from '../services/tickets'
import type { Ticket } from '../types/domain'

export function useTickets(): {
  tickets: Ticket[]
  reload: () => Promise<void>
  tick: number
} {
  const [tickets, setTickets] = useState<Ticket[]>([])
  // D-09: tick counter força re-render para atualizar tempo decorrido sem clonar array
  const [tick, setTick] = useState<number>(0)

  const reload = useCallback(async (): Promise<void> => {
    try {
      const data = await getTickets()
      setTickets(data)
    } catch (e) {
      console.error(e)
    }
  }, [])

  useEffect(() => {
    reload()
  }, [reload])

  useEffect(() => {
    const t = setInterval(() => setTick((n) => n + 1), 60000)
    return () => clearInterval(t)
  }, [])

  return { tickets, reload, tick }
}
```

**Criar `src/renderer/src/hooks/useGlobalShortcuts.ts`** com EXATAMENTE:

```typescript
import { useEffect } from 'react'
import type { View } from '../types/domain'

interface GlobalShortcutsOptions {
  view: View
  onCtrlN?: () => void
}

export function useGlobalShortcuts({ view, onCtrlN }: GlobalShortcutsOptions): void {
  useEffect(() => {
    const handleKeyDown = (e: KeyboardEvent): void => {
      if ((e.key === 'n' || e.key === 'N') && (e.ctrlKey || e.metaKey)) {
        if (view === 'mensalistas' && onCtrlN) {
          e.preventDefault()
          onCtrlN()
        }
      }
      // Escape é tratado localmente por cada modal/view (RESEARCH.md Open Question 2)
    }

    window.addEventListener('keydown', handleKeyDown)
    return () => window.removeEventListener('keydown', handleKeyDown)
  }, [view, onCtrlN])
}
```

Regras:
- Sem `;`, single quotes.
- Named exports apenas.
- Hook não importa React default.
- useTickets retorna `tick` para que views consumidoras possam usar como dep de useMemo se quiserem (não obrigatório).

**Decisão D-02 — hooks não criados nesta wave:**
D-02 listou `useClients`, `useFinancial` e `useDailyReport` como hooks opcionais "onde a complexidade de ciclo de vida justificar". Após análise das waves anteriores:
- `useClients` → estado inline em Mensalistas.tsx (01-04) — único consumidor, sem sharing entre views, complexidade não justifica hook separado.
- `useFinancial` → estado inline em Financeiro.tsx (01-03) — mesma justificativa.
- `useDailyReport` → estado inline em Relatorio.tsx (01-03) — mesma justificativa.
`useTickets` e `useGlobalShortcuts` são criados aqui porque têm ciclo de vida com setInterval e event listener global respectivamente — exatamente o critério de D-02. Os outros 3 são simples useEffect+fetch sem lifecycle especial, implementados inline conforme D-07 (services first).

NÃO modificar App.tsx ainda nesta task — apenas criar os 2 arquivos. A integração acontece nas tasks 2 e 3.
  </action>
  <acceptance_criteria>
    - `src/renderer/src/hooks/useTickets.ts` existe e contém `export function useTickets`
    - `grep -c "setInterval" src/renderer/src/hooks/useTickets.ts` retorna 1
    - `grep -c "clearInterval" src/renderer/src/hooks/useTickets.ts` retorna 1
    - `grep -c "from '../services/tickets'" src/renderer/src/hooks/useTickets.ts` retorna 1
    - `grep -c "window\\." src/renderer/src/hooks/useTickets.ts` retorna 0
    - `wc -l src/renderer/src/hooks/useTickets.ts` retorna número < 50
    - `src/renderer/src/hooks/useGlobalShortcuts.ts` existe e contém `export function useGlobalShortcuts`
    - `grep -c "addEventListener" src/renderer/src/hooks/useGlobalShortcuts.ts` retorna 1
    - `grep -c "removeEventListener" src/renderer/src/hooks/useGlobalShortcuts.ts` retorna 1
    - `wc -l src/renderer/src/hooks/useGlobalShortcuts.ts` retorna número < 40
    - Não existe `src/renderer/src/hooks/index.ts` (sem barrel)
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\Users\muril\OneDrive\Desktop\Estacionamento software\Projetos\meu-estacionamento" && npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <done>Os 2 hooks existem com a API correta; gates verdes; App.tsx não foi modificado nesta task.</done>
</task>

<task id="01-05-02" type="execute" tdd="false">
  <name>Task 2: Criar Inicio.tsx</name>
  <files>
    - src/renderer/src/views/Inicio.tsx (novo)
  </files>
  <read_first>
    - src/renderer/src/App.tsx (BLOCO COMPLETO de Inicio: linhas 691-917 do JSX; linhas 282-531 dos handlers; linhas 1707-1741 do garage modal inline; linhas 1743-1792 do debtor decision modal inline; linhas 1582-1635 do modal "Excluir todos" inline)
    - src/renderer/src/components/ModalCheckout.tsx (props para Inicio passar)
    - src/renderer/src/services/tickets.ts (todos os 8 exports)
    - src/renderer/src/services/printer.ts (printEntry, printExit)
    - src/renderer/src/hooks/useBarcodeScanner.ts (existente — Inicio consome)
    - src/renderer/src/hooks/useTickets.ts (criado em task 1)
    - src/renderer/src/providers/DialogProvider.tsx
    - src/renderer/src/utils/masks.ts (maskPlate, plateToRaw, validatePlate)
    - src/renderer/src/utils/errorHandler.ts (friendlyError)
    - .planning/phases/01-refatora-o-do-renderer/01-PATTERNS.md (seção "src/renderer/src/views/Inicio.tsx")
  </read_first>
  <action>
**Criar `src/renderer/src/views/Inicio.tsx`** — esta é a maior task da fase. O arquivo terá ~350 linhas. O shape geral:

```typescript
import { useState, useEffect, useCallback } from 'react'
import { format, differenceInMinutes } from 'date-fns'
import { clsx } from 'clsx'
import {
  createTicket,
  checkoutTicket,
  calculateValue,
  checkPlateSubscription,
  checkPlateWasInToday,
  excludeAllActiveTickets
} from '../services/tickets'
import { printEntry, printExit } from '../services/printer'
import ModalCheckout from '../components/ModalCheckout'
import { useTickets } from '../hooks/useTickets'
import { useBarcodeScanner } from '../hooks/useBarcodeScanner'
import { useDialog } from '../providers/DialogProvider'
import { maskPlate, plateToRaw, validatePlate } from '../utils/masks'
import { friendlyError } from '../utils/errorHandler'
import type { Ticket, SubscriptionInfo, View } from '../types/domain'
import logoImg from '../assets/logo.png'

interface InicioProps {
  setView: (v: View) => void
}

export default function Inicio({ setView }: InicioProps): React.JSX.Element {
  const { showAlert, showConfirm } = useDialog()
  const { tickets, reload: reloadTickets, tick } = useTickets()

  // Estado de input de placa
  const [placa, setPlaca] = useState<string>('')
  const [tipo, setTipo] = useState<string>('Carro')
  const [loading, setLoading] = useState<boolean>(false)
  const [subscriptionInfo, setSubscriptionInfo] = useState<SubscriptionInfo | null>(null)
  const [plateWasInToday, setPlateWasInToday] = useState<boolean>(false)
  const [searchPlacaList, setSearchPlacaList] = useState<string>('')

  // Fluxo devedor
  const [debtorDecisionOpen, setDebtorDecisionOpen] = useState<boolean>(false)
  const [pendingEntry, setPendingEntry] = useState<{ placa: string; tipo: string; subscriptionInfo: SubscriptionInfo } | null>(null)

  // Modal garagem
  const [garageEntryModal, setGarageEntryModal] = useState<{ open: boolean; placa: string; tipo: string }>({ open: false, placa: '', tipo: '' })

  // ModalCheckout
  const [modalOpen, setModalOpen] = useState<boolean>(false)
  const [checkoutTicketState, setCheckoutTicketState] = useState<Ticket | null>(null)
  const [checkoutValor, setCheckoutValor] = useState<number>(0)
  const [checkoutLoading, setCheckoutLoading] = useState<boolean>(false)

  // Modal "Excluir todos"
  const [modalExcluirTodosOpen, setModalExcluirTodosOpen] = useState<boolean>(false)
  const [excluirTodosPassword, setExcluirTodosPassword] = useState<string>('')
  const [excluirTodosLoading, setExcluirTodosLoading] = useState<boolean>(false)
  const [excluirTodosError, setExcluirTodosError] = useState<string>('')

  // Debounce para checkPlateWasInToday — copiar verbatim de App.tsx ~205
  useEffect(() => {
    const raw = plateToRaw(placa)
    if (!raw || raw.length < 7) {
      setPlateWasInToday(false)
      return
    }
    const t = setTimeout(() => {
      checkPlateWasInToday(raw).then(setPlateWasInToday).catch(console.error)
    }, 300)
    return () => clearTimeout(t)
  }, [placa])

  // handlePlacaBlur — copiar verbatim de App.tsx ~282
  const handlePlacaBlur = useCallback(async (): Promise<void> => {
    const raw = plateToRaw(placa)
    if (!raw) {
      setSubscriptionInfo(null)
      return
    }
    try {
      const info = await checkPlateSubscription(raw)
      setSubscriptionInfo(info)
    } catch (err) {
      console.error(err)
      setSubscriptionInfo(null)
    }
  }, [placa])

  // registerEntryWithType — copiar verbatim de App.tsx ~290-340
  const registerEntryWithType = useCallback(async (plate: string, vehicleType: string): Promise<void> => {
    setLoading(true)
    try {
      const result = await createTicket({ placa: plate, tipo: vehicleType })
      if (result.success && result.id) {
        // Anti-pattern eliminado: agora via service tipado
        const printRes = await printEntry({
          id: result.id,
          placa: plate.toUpperCase(),
          entrada: result.entrada ?? new Date().toISOString()
        })
        if (printRes && !printRes.success) {
          showAlert('Erro de impressão', friendlyError(printRes.error ?? 'printer'), 'error')
        }
        setPlaca('')
        setSubscriptionInfo(null)
        setPlateWasInToday(false)
        await reloadTickets()
        if (result.message) {
          showAlert('Atenção', result.message, 'success')
        }
      } else {
        showAlert('Erro', friendlyError(result.error ?? 'Não foi possível registrar entrada'), 'error')
      }
    } catch (err) {
      console.error(err)
      showAlert('Erro', friendlyError(err), 'error')
    } finally {
      setLoading(false)
    }
  }, [showAlert, reloadTickets])

  // handleRegisterEntry — tradução direta de App.tsx:463-500 com window.api → service imports
  // Branches CRÍTICOS — não simplificar:
  //   1. Sem placa → retorna imediatamente
  //   2. Placa curta (< 7 chars) → registerEntryWithType direto com tipo do select
  //   3. Mensalista em dia + devedor → abre debtorDecisionOpen (operador escolhe avulso ou mensalistas)
  //   4. Mensalista em dia + plano GARAGEM → abre garageEntryModal (confirmação de diária)
  //   5. Mensalista em dia + plano MENSAL → registra como 'MENSALISTA'
  //   6. Não-mensalista → registerEntryWithType com tipo do select
  const handleRegisterEntry = useCallback(async (): Promise<void> => {
    if (!placa.trim()) return
    setLoading(true)
    try {
      let tipoToSave: string = tipo
      if (plateToRaw(placa).length >= 7) {
        const info = await checkPlateSubscription(placa)
        if (info?.isSubscriber && !info?.isExpired) {
          if (
            info?.isDebtor &&
            (info?.planType?.startsWith('MENSAL') || info?.planType === 'GARAGEM')
          ) {
            setPendingEntry({ placa, tipo, subscriptionInfo: info })
            setDebtorDecisionOpen(true)
            setLoading(false)
            return
          }
          if (info?.planType === 'GARAGEM') {
            setGarageEntryModal({ open: true, placa, tipo: 'GARAGEM' })
            setSubscriptionInfo(info)
            setLoading(false)
            return
          }
          tipoToSave = 'MENSALISTA'
        }
        setSubscriptionInfo(info)
      }
      await registerEntryWithType(placa, tipoToSave)
    } catch (err) {
      console.error(err)
      showAlert('Erro', friendlyError(err), 'error')
    } finally {
      setLoading(false)
    }
  }, [placa, tipo, showAlert])

  // handleCheckoutClick — copiar verbatim de App.tsx ~340
  const handleCheckoutClick = useCallback(async (ticket: Ticket): Promise<void> => {
    setCheckoutTicketState(ticket)
    setModalOpen(true)
    try {
      const res = await calculateValue({ entrada: ticket.entrada, placa: ticket.placa, tipo: ticket.tipo })
      setCheckoutValor(res.valor)
    } catch (err) {
      console.error(err)
      setCheckoutValor(0)
    }
  }, [])

  // handleCheckoutConfirm — copiar verbatim de App.tsx ~360-380
  // ATENÇÃO: REMOVER o cross-view refetch (Pitfall 3 do RESEARCH.md):
  //   if (view === 'historico') ... — REMOVER (cada view refetcha no mount)
  //   if (view === 'financeiro') ... — REMOVER
  const handleCheckoutConfirm = useCallback(async (): Promise<void> => {
    if (!checkoutTicketState) return
    setCheckoutLoading(true)
    try {
      const res = await checkoutTicket({ id: checkoutTicketState.id })
      if (res.success && res.valor !== undefined) {
        // Anti-pattern eliminado:
        const tempoTotal = String(differenceInMinutes(new Date(), new Date(checkoutTicketState.entrada))) + ' min'
        const printRes = await printExit({
          placa: checkoutTicketState.placa,
          entrada: checkoutTicketState.entrada,
          saida: new Date().toISOString(),
          valor: res.valor,
          tempoTotal
        })
        if (printRes && !printRes.success) {
          showAlert('Erro de impressão', friendlyError(printRes.error ?? 'printer'), 'error')
        }
        setModalOpen(false)
        setCheckoutTicketState(null)
        setCheckoutValor(0)
        await reloadTickets()
      } else {
        showAlert('Erro', friendlyError(res.error ?? 'Não foi possível finalizar'), 'error')
      }
    } catch (err) {
      console.error(err)
      showAlert('Erro', friendlyError(err), 'error')
    } finally {
      setCheckoutLoading(false)
    }
  }, [checkoutTicketState, showAlert, reloadTickets])

  // handleBarcodeScanned — copiar verbatim de App.tsx ~514, mas SIMPLIFICAR deps (Pitfall 5):
  // Sem `view` na dep array (a view sempre é 'inicio' aqui)
  const handleBarcodeScanned = useCallback((value: string): void => {
    const scanned = plateToRaw(value)
    if (!scanned) return
    const ticket = tickets.find((t) => plateToRaw(t.placa ?? '') === scanned)
    if (ticket) {
      setPlaca('')
      setSearchPlacaList('')
      void handleCheckoutClick(ticket)
    } else {
      showAlert('Placa não encontrada', `Nenhum veículo estacionado com "${maskPlate(scanned)}"`, 'error')
    }
  }, [tickets, handleCheckoutClick, showAlert])

  useBarcodeScanner(handleBarcodeScanned, true)

  // handleExcluirTodos — copiar verbatim de App.tsx ~410
  const handleExcluirTodosConfirm = useCallback(async (): Promise<void> => {
    setExcluirTodosLoading(true)
    setExcluirTodosError('')
    try {
      const res = await excludeAllActiveTickets({ password: excluirTodosPassword })
      if (res.success) {
        setModalExcluirTodosOpen(false)
        setExcluirTodosPassword('')
        await reloadTickets()
        showAlert('Sucesso', 'Todos os tickets ativos foram excluídos.', 'success')
      } else {
        setExcluirTodosError(res.error ?? 'Senha incorreta')
      }
    } catch (err) {
      setExcluirTodosError(friendlyError(err))
    } finally {
      setExcluirTodosLoading(false)
    }
  }, [excluirTodosPassword, reloadTickets, showAlert])

  return (
    <div className="flex-1 p-6 overflow-y-auto">
      {/* JSX completo de App.tsx:691-917 — copiar verbatim:
          - Header com logo
          - Card de entrada de placa: input placa, select tipo, info de assinatura
          - Botão "Registrar entrada" → onClick={handleRegisterEntry}
          - Botão "Excluir todos" → onClick={() => setModalExcluirTodosOpen(true)}
          - Lista de tickets ativos (mapear tickets):
              - Filtrar por searchPlacaList se não vazio
              - Cada card mostra placa, tipo, entrada, tempo decorrido (use `tick` ou differenceInMinutes(new Date(), entrada))
              - Botão "Saída" → onClick={() => handleCheckoutClick(t)}
          - tick é usado IMPLÍCITAMENTE como dep para forçar re-render do tempo decorrido
      */}

      <ModalCheckout
        isOpen={modalOpen}
        onClose={() => {
          setModalOpen(false)
          setCheckoutTicketState(null)
          setCheckoutValor(0)
        }}
        ticket={checkoutTicketState}
        valor={checkoutValor}
        loading={checkoutLoading}
        onConfirm={handleCheckoutConfirm}
      />

      {/* debtorDecisionOpen modal inline — copiar verbatim de App.tsx:1743-1792:
          - {debtorDecisionOpen && pendingEntry && ( ... )}
          - 2 opções: "Cobrar avulso" (registerEntryWithType com tipo avulso) ou "Marcar como pago e ir para Mensalistas" (setView('mensalistas'))
          - Botão "Cancelar" fecha o modal
      */}

      {/* garageEntryModal inline — copiar verbatim de App.tsx:1707-1741:
          - {garageEntryModal.open && ( ... )}
          - Confirma o uso da diária de garagem
          - Botão Confirmar → registerEntryWithType com tipo GARAGEM
      */}

      {/* modalExcluirTodos inline — copiar verbatim de App.tsx:1582-1635:
          - {modalExcluirTodosOpen && ( ... )}
          - Input de senha + botões Confirmar/Cancelar
          - Mostra excluirTodosError quando truthy
      */}
    </div>
  )
}
```

**ATENÇÃO (críticas — leia antes de implementar):**

1. **Lógica de `handleRegisterEntry`** é o coração do produto e tem múltiplos branches:
   - Validar placa via `validatePlate(plate)` antes de tudo
   - `checkPlateSubscription(plate)` retorna `subscriptionInfo`
   - Se `subscriptionInfo.isSubscriber === true` AND não devedor AND não expirado → `registerEntryWithType` direto com o tipo do plano (Carro/Moto). Se for plano GARAGEM → abrir `garageEntryModal`.
   - Se `subscriptionInfo.isDebtor === true` OR `subscriptionInfo.isExpired === true` → abrir `debtorDecisionOpen` (operador escolhe cobrar avulso ou mandar para Mensalistas)
   - Caso contrário (não-mensalista) → `registerEntryWithType` com `tipo` do select (Carro/Moto)
   - **Copiar EXATAMENTE de App.tsx ~290-330** — cada branch e cada mensagem de erro/sucesso tem que ser idêntico. Não tentar simplificar.

2. **`debtorDecisionOpen` modal**: 2 botões — "Cobrar avulso" (chama `registerEntryWithType(placa, 'Carro' ou 'Moto')` ignorando o subscriptionInfo) e "Cobrar mensalidade pendente" (chama `setView('mensalistas')` para o operador resolver o débito). Cópia verbatim do JSX de App.tsx:1743-1792.

3. **`garageEntryModal`**: 1 botão Confirmar que chama `registerEntryWithType(placa, 'GARAGEM')`. Cópia verbatim de App.tsx:1707-1741.

4. **`modalExcluirTodos`**: idêntico ao deleteClientModal de Mensalistas — input password + botões Confirmar/Cancelar + mostrar error quando truthy. Cópia verbatim de App.tsx:1582-1635.

5. **`tick` do useTickets**: O valor é exposto pelo hook. Se o JSX original computa "X minutos" via `differenceInMinutes(new Date(), entrada)`, esse cálculo precisa re-rodar a cada minuto — basta o tick estar como dep de algum useMemo ou simplesmente referenciar `tick` no JSX (mesmo que não use o valor — TypeScript pode reclamar, então usar `void tick` ou inserir como hidden span). PATTERNS.md sugere usar como dep de useMemo da lista filtrada. ESCOLHER UMA: ou colocar `tick` como dep de um useMemo de tickets filtrados, ou simplesmente referenciar `void tick` no início do componente para o linter aceitar a "dependência não usada". Documentar a escolha em comentário.

6. **Strings em pt-BR copiadas verbatim**.

7. **Sem `;`, single quotes**.

8. **Cleanup de imports**: importar logoImg apenas se for usado no JSX; remover qualquer import que não estiver sendo referenciado.

NÃO modificar App.tsx nesta task — apenas criar Inicio.tsx. A migração de App.tsx é a task 3.
  </action>
  <acceptance_criteria>
    - `src/renderer/src/views/Inicio.tsx` existe
    - `grep -c "export default function Inicio" src/renderer/src/views/Inicio.tsx` retorna 1
    - `grep -c "InicioProps" src/renderer/src/views/Inicio.tsx` retorna pelo menos 1 (interface declarada)
    - `grep -c "setView" src/renderer/src/views/Inicio.tsx` retorna pelo menos 1 (uso da prop)
    - `grep -c "useBarcodeScanner" src/renderer/src/views/Inicio.tsx` retorna 1
    - `grep -c "useTickets" src/renderer/src/views/Inicio.tsx` retorna 1
    - `grep -c "useDialog" src/renderer/src/views/Inicio.tsx` retorna 1
    - `grep -c "<ModalCheckout" src/renderer/src/views/Inicio.tsx` retorna 1
    - `grep -c "createTicket\\|checkoutTicket\\|calculateValue\\|checkPlateSubscription\\|checkPlateWasInToday\\|excludeAllActiveTickets" src/renderer/src/views/Inicio.tsx` retorna pelo menos 6
    - `grep -c "printEntry\\|printExit" src/renderer/src/views/Inicio.tsx` retorna pelo menos 2
    - `grep -c "window\\." src/renderer/src/views/Inicio.tsx` retorna 0 (regressão zero)
    - `grep -c "ipcRenderer" src/renderer/src/views/Inicio.tsx` retorna 0
    - `wc -l src/renderer/src/views/Inicio.tsx` retorna número < 400 (regra REF-01)
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
    - `npm test` exits 0
    - App.tsx ainda contém o bloco original de Inicio nesta task (não foi modificado): `grep -c "view === 'inicio'" src/renderer/src/App.tsx` retorna pelo menos 1
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\Users\muril\OneDrive\Desktop\Estacionamento software\Projetos\meu-estacionamento" && npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <done>Inicio.tsx existe e compila com toda a lógica copiada verbatim; gates verdes; App.tsx ainda intocado (próxima task faz a migração).</done>
</task>

<task id="01-05-03" type="execute" tdd="false">
  <name>Task 3: Migrar App.tsx para usar Inicio + hooks + cleanup final</name>
  <files>
    - src/renderer/src/App.tsx (refactor profundo — encolhe para ~150-200 linhas)
  </files>
  <read_first>
    - src/renderer/src/App.tsx (estado atual completo após 01-04 — todas as ~1200 linhas restantes)
    - src/renderer/src/views/Inicio.tsx (criada em task 2)
    - src/renderer/src/hooks/useTickets.ts (criada em task 1)
    - src/renderer/src/hooks/useGlobalShortcuts.ts (criada em task 1)
    - src/renderer/src/views/Historico.tsx, Relatorio.tsx, Mensalistas.tsx, Financeiro.tsx, Excluidos.tsx, Configuracoes.tsx (já existem das waves anteriores)
    - .planning/phases/01-refatora-o-do-renderer/01-PATTERNS.md (todas as integrações)
  </read_first>
  <action>
**Refactor cirúrgico de `src/renderer/src/App.tsx` — o objetivo é deixá-lo no formato:**

```typescript
import { useState, useRef } from 'react'
import { clsx } from 'clsx'
import logoImg from './assets/logo.png'
import Inicio from './views/Inicio'
import Historico from './views/Historico'
import Relatorio from './views/Relatorio'
import Mensalistas, { type MensalistasHandle } from './views/Mensalistas'
import Financeiro from './views/Financeiro'
import Excluidos from './views/Excluidos'
import Configuracoes from './views/Configuracoes'
import { useGlobalShortcuts } from './hooks/useGlobalShortcuts'
import type { View } from './types/domain'

export default function App(): React.JSX.Element {
  const [view, setView] = useState<View>('inicio')
  const mensalistasRef = useRef<MensalistasHandle>(null)

  useGlobalShortcuts({
    view,
    onCtrlN: () => mensalistasRef.current?.openNewClientModal()
  })

  return (
    <div className="min-h-screen bg-gray-900 text-white flex">
      {/* Sidebar (manter JSX da sidebar original — botões para cada view com clsx active) */}
      <aside className="w-64 bg-gray-800 ...">
        {/* Logo + 7 botões de navegação:
            - Início, Histórico, Relatório, Mensalistas, Financeiro, Excluídos, Configurações
            - Cada botão: onClick={() => setView('inicio'|'historico'|...)}
            - Estilo ativo: clsx('base-classes', view === 'X' && 'bg-red-600')
        */}
      </aside>

      {/* Main content */}
      <main className="flex-1 flex flex-col">
        {view === 'inicio' && <Inicio setView={setView} />}
        {view === 'historico' && <Historico />}
        {view === 'relatorio' && <Relatorio />}
        {view === 'mensalistas' && <Mensalistas ref={mensalistasRef} />}
        {view === 'financeiro' && <Financeiro />}
        {view === 'excluidos' && <Excluidos />}
        {view === 'configuracoes' && <Configuracoes />}
      </main>
    </div>
  )
}
```

**Para chegar nesse estado, REMOVER de App.tsx:**

1. **Todos os useStates restantes** (que não são `view`): tickets, modalOpen, checkoutTicket, checkoutValor, checkoutLoading, modalExcluirTodosOpen, excluirTodosPassword, excluirTodosLoading, excluirTodosError, placa, tipo, loading, subscriptionInfo, plateWasInToday, searchPlacaList, debtorDecisionOpen, pendingEntry, garageEntryModal, e qualquer outro state que sobrou.

2. **Todos os useEffects** restantes: setInterval hack (D-09 — agora em useTickets), loadTickets, debounce checkPlateWasInToday, keyboard listener (Ctrl+N agora em useGlobalShortcuts), e qualquer outro effect ainda presente.

3. **Todos os handlers restantes**: handlePlacaBlur, handleRegisterEntry, registerEntryWithType, handleCheckoutClick, handleCheckoutConfirm, handleBarcodeScanned, openExcluirTodosConfirm, etc. — todos foram movidos para Inicio.tsx.

4. **JSX inline da view 'inicio'** (linhas ~691-917) e dos modais inline (debtorDecision ~1743-1792, garage ~1707-1741, excluirTodos ~1582-1635) — todos foram movidos para Inicio.tsx.

5. **Imports não usados**: ModalCheckout, useBarcodeScanner, todos os imports de services exceto se algum continua usado pela sidebar (improvável). Manter apenas: React (useState, useRef), clsx, logoImg, os 7 imports de views, useGlobalShortcuts, e tipo View.

6. **Confirmar que o JSX da sidebar permanece intacto** — o switch de views agora tem o render condicional em vez do JSX inline, mas a sidebar (botões de navegação, logo, estilo) é a mesma do original.

**MANTER em App.tsx:**

- `useState<View>('inicio')` — o roteador
- `useRef<MensalistasHandle>(null)` — para Ctrl+N
- `useGlobalShortcuts({ view, onCtrlN })` — substitui o useEffect de keyboard
- JSX da sidebar (com botões de navegação)
- Switch de render condicional para as 7 views

**Regras:**
- Sem `;`, single quotes (Prettier).
- Após o cleanup, App.tsx deve ficar abaixo de 200 linhas (estimativa: 100-150).
- A sidebar permanece em App.tsx por enquanto — extrair sidebar para um componente próprio é over-engineering nesta fase (D-08 em CONTEXT.md: ~400 linhas é guideline, não obrigação para sidebar pequena).
- ZERO chamadas `window.api.*` no arquivo (já era assim desde 01-02).
- ZERO chamadas `window.electron.ipcRenderer.*` no arquivo.
- Imports limpos (sem warnings de unused).

**Verificações finais antes de fechar a task:**
- O Ctrl+N na view Mensalistas deve continuar abrindo o modal Novo Cliente (via mensalistasRef.current?.openNewClientModal())
- O Escape para fechar modais não está em App — cada modal/view trata localmente. Se algo quebrar, registrar em SUMMARY (provável que continue funcionando porque AlertModal já trata Escape internamente)
- Todos os 7 botões da sidebar trocam view corretamente
  </action>
  <acceptance_criteria>
    - `wc -l src/renderer/src/App.tsx` retorna número < 200
    - `grep -c "useState" src/renderer/src/App.tsx` retorna 1 (apenas o `useState<View>`)
    - `grep -c "useEffect" src/renderer/src/App.tsx` retorna 0 (todos os effects movidos)
    - `grep -c "useGlobalShortcuts" src/renderer/src/App.tsx` retorna 1
    - `grep -c "<Inicio" src/renderer/src/App.tsx` retorna 1
    - `grep -c "<Historico" src/renderer/src/App.tsx` retorna 1
    - `grep -c "<Relatorio" src/renderer/src/App.tsx` retorna 1
    - `grep -c "<Mensalistas" src/renderer/src/App.tsx` retorna 1
    - `grep -c "<Financeiro" src/renderer/src/App.tsx` retorna 1
    - `grep -c "<Excluidos" src/renderer/src/App.tsx` retorna 1
    - `grep -c "<Configuracoes" src/renderer/src/App.tsx` retorna 1
    - `grep -c "window\\." src/renderer/src/App.tsx` retorna 0
    - `grep -c "ipcRenderer" src/renderer/src/App.tsx` retorna 0
    - `grep -c "import.*from '\\./services/" src/renderer/src/App.tsx` retorna 0 (App não chama services direto — todas as chamadas vão por views)
    - `grep -c "<AlertModal\\|<ModalCheckout\\|<ModalNovoCliente\\|<ModalRenovar" src/renderer/src/App.tsx` retorna 0 (modais movidos para suas views)
    - **Verificação final REF-02 (todo o renderer):** `grep -rn "window\\.electron\\.ipcRenderer" src/renderer/src/` retorna 0 matches
    - **Verificação final REF-02 (fora de services):** `grep -rn "window\\.api\\." src/renderer/src/views/ src/renderer/src/components/ src/renderer/src/hooks/ src/renderer/src/providers/` retorna 0 matches (window.api só vive em services/)
    - **Verificação final REF-01:** todos os arquivos `*.tsx` em `src/renderer/src/views/` e `src/renderer/src/App.tsx` ficam abaixo de 400 linhas. Verificar com `find src/renderer/src -name "*.tsx" -not -path "*/node_modules/*" | xargs wc -l | sort -rn | head -10` — primeiro arquivo deve ter < 400 linhas
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
    - `npm test` exits 0
    - `npm run build` exits 0 (sanity check do bundle final)
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\Users\muril\OneDrive\Desktop\Estacionamento software\Projetos\meu-estacionamento" && npm run typecheck && npm run lint && npm test && npm run build</automated>
  </verify>
  <done>App.tsx reduzido a < 200 linhas com sidebar + dispatch + Ctrl+N hook; todas as 7 views renderizadas via switch; zero IPC direto em components; build limpo; UAT manual passa em todos os fluxos críticos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inicio.tsx ↔ services/printer (printEntry, printExit) | Substituição final dos 2 anti-patterns `window.electron.ipcRenderer.invoke('print-entry'/'print-exit')`. Falha aqui = ticket é gerado sem imprimir, OU duplicado. |
| Inicio.tsx ↔ debtorDecision flow | Lógica crítica do produto: decidir entre cobrar avulso e mandar para Mensalistas. Bug = cliente devedor entra grátis ou é cobrado duplo. |
| App.tsx ↔ Mensalistas via mensalistasRef | Ctrl+N depende de ref bem conectada. Falha = atalho de teclado some. |
| useTickets (setInterval tick) | Substituição do hack `setInterval(setTickets((p) => [...p]), 60000)` por `setTick((n) => n+1)`. Falha = tempo decorrido na lista de ativos para de atualizar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Lógica de `handleRegisterEntry` perder um branch (ex: deixar de abrir debtorDecision para devedor, deixando-o entrar grátis) | mitigate | Action instrui copiar VERBATIM os branches de App.tsx:290-330. UAT manual obrigatório (checklist abaixo) com casos: avulso, mensalista em dia, mensalista expirado, mensalista devedor, garagem. |
| T-05-02 | Tampering | printEntry/printExit chamados com payload em ordem errada (ex: id e placa trocados) | mitigate | Tipos no preload (criados em 01-01) forçam estrutura correta. typecheck cobre. UAT manual: imprimir um ticket de entrada e um de saída e conferir o conteúdo do papel. |
| T-05-03 | Repudiation | Ticket é registrado mas impressão falha silenciosamente | mitigate | `printRes && !printRes.success` dispara showAlert "Erro de impressão" — mesmo comportamento do código original. Operador vê o erro e sabe que precisa imprimir manualmente (ou via histórico). |
| T-05-04 | Denial of Service | useTickets `setInterval` permanece ativo após unmount (memory leak) | mitigate | `return () => clearInterval(t)` no useEffect cleanup. Padrão verificado em useBarcodeScanner.ts. |
| T-05-05 | Information Disclosure | Senha de "Excluir todos" exibida em log/console | accept | Senha vai para o IPC handler em main; comportamento idêntico ao original. Não há nova superfície. |
| T-05-06 | Elevation of Privilege | Inicio.tsx ou App.tsx final regredir com `window.api` ou `window.electron` direto | mitigate | Acceptance criteria com grep gates específicos. Verificação final cobre TODO o renderer (`grep -rn ... src/renderer/src/`). |
| T-05-07 | Tampering | `handleBarcodeScanned` com `view` na dep array depois da extração (Pitfall 5) | mitigate | Action instrui REMOVER `view` da dep — view sempre é 'inicio' dentro do componente. Lint react-hooks/exhaustive-deps verifica. |
| T-05-08 | Tampering | Cross-view refetch removido de handleCheckoutConfirm (Pitfall 3) mas Historico/Financeiro não estão recebendo dados atualizados após checkout | accept | Solução documentada: cada view refetcha no mount via useEffect. UAT testa: fazer checkout em Inicio → trocar para Historico → confirmar que o ticket finalizado aparece. Risco: se o operador NÃO trocar de view após checkout, a view atual (Inicio) já está atualizada via reloadTickets; outras views só atualizam quando navegadas. Aceitável e idêntico ao comportamento de qualquer SPA bem feita. |
| T-05-09 | Denial of Service | App.tsx final ficar com algum import circular ou symbol não exportado | mitigate | `npm run typecheck` cobre todo o grafo de imports. `npm run build` é o gate final. |
| T-05-10 | Tampering | Sidebar perder algum botão durante o cleanup do JSX | mitigate | Acceptance criteria força que os 7 imports de view existam e os 7 renders condicionais (`<Inicio`, `<Historico`, ..., `<Configuracoes`) estejam presentes. UAT manual testa cada botão. |
| T-05-11 | Tampering | tick do useTickets não dispara re-render (operador vê "0 min" parado) | mitigate | `tick` é `useState` — qualquer mudança força re-render do componente. Se Inicio.tsx não usa `tick` em lugar nenhum (nem como dep de useMemo, nem referenciado no JSX), o re-render acontece mesmo assim porque o useState muda no escopo do componente que renderiza Inicio. UAT manual: deixar a tela aberta por 1+ minuto e ver "X min" subir. |

## Mitigations

| Threat | Mitigation |
|--------|-----------|
| T-05-01, T-05-02, T-05-08, T-05-10, T-05-11 | UAT manual obrigatório (checklist completo abaixo) |
| T-05-04 | useEffect cleanup obrigatório no useTickets (verificado por inspeção do código) |
| T-05-06 | grep gates em todo o renderer no acceptance criteria final |
| T-05-07 | lint react-hooks/exhaustive-deps |
| T-05-09 | npm run build no gate final |
</threat_model>

<verification>
**Gate por task:** `npm run typecheck && npm run lint && npm test`. Task 3 também roda `npm run build`.

**Gate de wave (após as 3 tasks) — verificações finais REF-01, REF-02, REF-03:**

```bash
cd "C:\Users\muril\OneDrive\Desktop\Estacionamento software\Projetos\meu-estacionamento"

# Gates obrigatórios
npm run typecheck
npm run lint
npm test
npm run build

# REF-02 final: zero anti-pattern em todo o renderer
grep -rn "window\.electron\.ipcRenderer" src/renderer/src/                  # esperado: 0
grep -rn "window\.api\." src/renderer/src/views/ src/renderer/src/components/ src/renderer/src/hooks/ src/renderer/src/providers/  # esperado: 0
# window.api só pode aparecer em src/renderer/src/services/
grep -rn "window\.api\." src/renderer/src/services/ | wc -l                 # esperado: >= 30 (todos os wrappers)

# REF-01 final: todos arquivos .tsx do renderer abaixo de 400 linhas
find src/renderer/src -name "*.tsx" | xargs wc -l | sort -rn | head -10
# Inspecionar visualmente: primeira linha (maior arquivo) deve ter < 400 linhas

# App.tsx final
wc -l src/renderer/src/App.tsx                                              # esperado: < 200

# 7 views existem
ls src/renderer/src/views/{Inicio,Historico,Relatorio,Mensalistas,Financeiro,Excluidos,Configuracoes}.tsx
# todos devem existir

# Hooks existem
ls src/renderer/src/hooks/{useTickets,useGlobalShortcuts,useBarcodeScanner}.ts

# Sem barrel files
test ! -f src/renderer/src/views/index.ts
test ! -f src/renderer/src/services/index.ts
test ! -f src/renderer/src/hooks/index.ts
test ! -f src/renderer/src/providers/index.ts

# REF-03: testes existentes verdes
npm test
```

**UAT manual obrigatório (REF-03 — checklist completo da fase, conforme TESTES-ANTES-DO-PENDRIVE.md):**

1. `npm run dev` — app abre sem erros no console
2. **Entrada de placa avulsa** (Carro): digitar placa nova → "Registrar entrada" → ticket aparece na lista de ativos com tempo "0 min" → impressão dispara (ou erro de impressão visível)
3. **Entrada de placa avulsa** (Moto): mesmo fluxo com tipo Moto
4. **Entrada de mensalista em dia**: digitar placa de mensalista ativo → assinatura info aparece (nome do cliente) → "Registrar entrada" sem prompts → ticket gerado
5. **Entrada de mensalista expirado**: digitar placa de mensalista expirado → "Registrar entrada" → modal de débito abre → escolher "Cobrar avulso" → ticket gerado como avulso
6. **Entrada de mensalista expirado, opção 2**: digitar placa, modal abre → escolher "Cobrar mensalidade pendente" → app navega para Mensalistas (setView funcionou)
7. **Entrada de garagem**: digitar placa de cliente com plan_type=GARAGEM → modal de garagem abre → confirmar → ticket gerado como GARAGEM
8. **Saída** (placa avulsa, dentro do tempo grátis): clicar "Saída" no card → modal de checkout aparece com R$ 0,00 → confirmar → ticket sai da lista, impressão dispara
9. **Saída** (placa avulsa, fora do tempo grátis): cobrança calculada > 0 → confirmar → ticket finalizado
10. **Tempo decorrido atualiza**: deixar a lista aberta por 1 minuto → confirmar que o "X min" sobe (regressão T-05-11)
11. **Barcode scanner**: simular leitura de código de barras (typing rápido) → se houver ticket ativo com a placa, abre modal de checkout (regressão T-05-07)
12. **Excluir todos os ativos**: clicar botão → modal de senha abre → senha errada → erro aparece → senha certa (senha de admin configurada em produção — obter via processo seguro, não registrada neste documento) → todos os tickets ativos saem da lista
13. **Histórico**: navegar → toggle 24h → trocar data → busca por placa
14. **Relatório**: trocar data → Salvar → Baixar PDF
15. **Mensalistas**: criar/editar/cancelar/reativar/renovar/extrato/excluir → todos funcionam
16. **Financeiro**: trocar mês/ano → exportar CSV
17. **Excluídos**: lista carrega
18. **Configurações**: trocar impressora → Salvar → alert de sucesso
19. **Ctrl+N na view Mensalistas**: pressionar → modal Novo Cliente abre (regressão T-04-05 + T-05-10)
20. **Cross-view refetch verificado** (T-05-08): finalizar um ticket em Inicio → navegar para Histórico → confirmar que aparece na lista
21. **Cross-view refetch verificado**: finalizar um ticket avulso → navegar para Financeiro → confirmar que aparece em "transações mistas"
</verification>

<success_criteria>
1. `src/renderer/src/views/Inicio.tsx` existe, < 400 linhas, sem `window.api` direto, com todos os fluxos críticos (entrada, saída, barcode, devedor, garagem, excluir todos).
2. `src/renderer/src/hooks/useTickets.ts` e `src/renderer/src/hooks/useGlobalShortcuts.ts` existem.
3. `src/renderer/src/App.tsx` < 200 linhas, contém apenas: `useState<View>`, `useRef<MensalistasHandle>`, `useGlobalShortcuts`, JSX de sidebar, e switch de 7 views.
4. **REF-01 fechado:** todo arquivo .tsx em `src/renderer/src/` < 400 linhas.
5. **REF-02 fechado:** `grep -rn "window\\.electron\\.ipcRenderer" src/renderer/src/` retorna 0; `grep -rn "window\\.api\\." src/renderer/src/views/ ... src/renderer/src/providers/` retorna 0.
6. **REF-03 fechado:** typecheck/lint/test/build verdes; UAT manual de 21 itens passa.
7. Anti-pattern de `window.electron.ipcRenderer.invoke('print-entry'/'print-exit')` totalmente eliminado.
</success_criteria>

<output>
Após completar todas as tasks:

`.planning/phases/01-refatora-o-do-renderer/01-05-SUMMARY.md`

Listar:
- Linhas do Inicio.tsx (real)
- Linhas finais de App.tsx (real)
- Top 5 maiores arquivos do renderer (com `find ... | xargs wc -l | sort -rn | head -5`) — todos devem estar < 400
- Status do UAT manual (21 itens)
- Confirmação de que `npm run build` saiu limpo
- REF-01, REF-02, REF-03 todos com status "DONE"

Após este SUMMARY, a fase 1 está COMPLETA. Próximo passo (fora desta fase): `/gsd-verify-work` para fechamento formal e migração para Phase 2 (Saneamento do Repositório).
</output>
